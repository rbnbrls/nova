"""Outbound message dispatcher — routes sends to the user's last-active channel.

Skeleton only. Phase 16 replaces with real dispatcher logic.
"""
from __future__ import annotations

# TODO(Phase 16): Implement send_to_user(user_name, text) that
# resolves last_active_channel from user_preferences and delegates
# to the correct ChannelAdapter. Move DND queue processing here.
