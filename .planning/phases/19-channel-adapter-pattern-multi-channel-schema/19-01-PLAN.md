---
phase: 19-channel-adapter-pattern-multi-channel-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/nova-core/alembic/versions/0007_drop_whatsapp_verification_codes.py
  - services/nova-core/app/channels/__init__.py
  - services/nova-core/app/channels/whatsapp.py
  - services/nova-core/app/channels/telegram.py
  - services/nova-core/app/channels/webhook_router.py
autonomous: true
requirements:
  - CHAN-01
  - CHAN-02
user_setup: []

must_haves:
  truths:
    - Schema has no WhatsApp-only tables — all verification codes go through multi-channel `channel_verification_codes`
    - ChannelAdapter ABC declares `register_webhooks` as a formal interface method
    - WhatsAppAdapter and TelegramAdapter both implement the full ABC contract
    - Existing WhatsApp webhook tests and outbound tests pass unchanged (pre-existing failures excluded)
    - `webhook_router.py` skeleton references Phase 20, not Phase 14
  artifacts:
    - services/nova-core/alembic/versions/0007_drop_whatsapp_verification_codes.py
    - services/nova-core/app/channels/__init__.py (updated with register_webhooks)
    - services/nova-core/app/channels/whatsapp.py (updated with register_webhooks stub)
    - services/nova-core/app/channels/telegram.py (updated with register_webhooks stub)
  key_links:
    - ChannelAdapter ABC -> adapter implementations (WhatsAppAdapter, TelegramAdapter)
    - `register_webhooks` -> main.py route registration will consolidate through this interface in Phase 20
    - Migration 0007 -> removes dead table, SC3 is gated on this DDL change
---

<objective>
Complete the multi-channel schema generalization and formalize the ChannelAdapter ABC so that WhatsApp and future channels (Telegram in Phase 20) share a common interface including webhook registration.

Purpose: All 6 Success Criteria from ROADMAP describe structural/infrastructure work that most schema already satisfies. This plan verifies SC1/SC2/SC4 are already compliant, adds the missing migration for SC3 (drop dead table), and adds register_webhooks to the ABC for SC5/SC6.

Output: Migration 0007 dropped table, ABC formalized with webhook-registration method, WhatsApp/Telegram adapters conforming fully, webhook_router TODO corrected to Phase 20.
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-channel-adapter-pattern-multi-channel-schema/19-CONTEXT.md
@services/nova-core/app/channels/__init__.py
@services/nova-core/app/channels/whatsapp.py
@services/nova-core/app/channels/telegram.py
@services/nova-core/app/channels/webhook_router.py
@services/nova-core/app/channels/dispatcher.py
@services/nova-core/alembic/versions/0001_initial_schema.py
@services/nova-core/alembic/versions/0006_add_memory_scope.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop unused whatsapp_verification_codes table via Alembic migration 0007</name>
  <files>
    services/nova-core/alembic/versions/0007_drop_whatsapp_verification_codes.py (NEW)
  </files>
  <action>
    Create Alembic migration 0007_drop_whatsapp_verification_codes.py that revises "0006" (the latest current migration, which added memory scope). The upgrade() function drops the whatsapp_verification_codes table via op.drop_table(). The downgrade() function recreates it with the identical schema from 0001_initial_schema.py lines 94-105: columns id (UUID, PK, server_default uuid_generate_v4), user_id (UUID, FK to users.id ON DELETE CASCADE, NOT NULL), whatsapp_number (Text, NOT NULL), code (Text, NOT NULL), attempts (Integer, server_default 0), expires_at (DateTime with timezone, NOT NULL), created_at (DateTime with timezone, server_default now()). Use op.create_table() with sa.Column() calls, matching the existing migration style exactly — do NOT use raw SQL.

    The whatsapp_verification_codes table was created in the initial migration (0001) but is never referenced by any application code. All code paths use channel_verification_codes which already has channel + channel_id columns for multi-channel support. Dropping this dead table completes SC3: "channel_verification_codes table generalizes the previous WhatsApp-specific table."

    After creating the file, grep for "whatsapp_verification_codes" across all .py files in services/nova-core/app/ (NOT in alembic/). Confirm zero matches outside the migrations directory. This proves no app code references the dead table.

    Do NOT modify any other files. This migration is additive-drop only — safe because the table has zero rows in production (never written to).
  </action>
  <verify>
    <automated>grep -rl 'whatsapp_verification_codes' services/nova-core/app/ 2>/dev/null; echo "grep exit: $? (expect 1 = no matches)"</automated>
    <automated>python -c "import sys; sys.path.insert(0, 'services/nova-core'); from alembic.config import Config; from alembic.script import ScriptDirectory; import os; d = os.path.join('services/nova-core', 'alembic'); c = Config(os.path.join(d, 'alembic.ini')); s = ScriptDirectory.from_config(c); heads = s.get_heads(); print('heads:', heads); assert len(heads) == 1, f'Expected 1 head, got {heads}'"</automated>
  </verify>
  <done>
    - Migration 0007 file exists at the expected path
    - upgrade() drops whatsapp_verification_codes, downgrade() recreates it
    - No app code references whatsapp_verification_codes
    - Alembic heads resolve to a single head (0007)
  </done>
</task>

<task type="auto">
  <name>Task 2: Formalize ChannelAdapter ABC with register_webhooks abstract method</name>
  <files>
    services/nova-core/app/channels/__init__.py (MODIFY)
    services/nova-core/app/channels/whatsapp.py (MODIFY)
    services/nova-core/app/channels/telegram.py (MODIFY)
  </files>
  <action>
    The ChannelAdapter ABC in __init__.py documents "Registers any webhook routes on the FastAPI app" in its class docstring but has no corresponding abstract method. Add register_webhooks to complete the formal contract.

    In __init__.py:
    - Add TYPE_CHECKING to the existing "from typing" import line (currently "from typing import Any"). Change it to: "from typing import TYPE_CHECKING, Any". Add a TYPE_CHECKING block after the dataclass import: inside it, "from fastapi import FastAPI".
    - Add a new abstract method to ChannelAdapter, placed BEFORE send_message (registration precedes message processing in lifecycle). Signature: "async def register_webhooks(self, app: FastAPI) -> None". Docstring: "Register webhook routes on the FastAPI application. Each channel adapter defines its own webhook route(s) under /webhooks/* and attaches them to the provided FastAPI app instance. The method is called during application startup. Args: app: The FastAPI application instance to attach routes to." Body should be "..." (Ellipsis) consistent with other abstract methods in the class.
    - Decorate with @abstractmethod.

    In whatsapp.py:
    - The file already has "from __future__ import annotations" at line 7.
    - Add TYPE_CHECKING import guard. The file currently has "from typing import Any" on line 10. Change it to "from typing import TYPE_CHECKING, Any". Add a conditional block: "if TYPE_CHECKING: from fastapi import FastAPI".
    - Add register_webhooks method to WhatsAppAdapter class. Place it as the first method (before send_message). Signature: "async def register_webhooks(self, app: FastAPI) -> None". Docstring: "Webhook routes are registered in main.py at /webhooks/whatsapp. Route migration to adapter method deferred to Phase 20." Body: "pass" (no-op stub).

    In telegram.py:
    - The file currently has "from typing import Any" on line 10. Change it to "from typing import TYPE_CHECKING, Any". Add the same TYPE_CHECKING block: "if TYPE_CHECKING: from fastapi import FastAPI".
    - Add register_webhooks method to TelegramAdapter as the first method. Same signature and docstring pattern as WhatsAppAdapter but referencing /webhooks/telegram. Body: "pass".

    Do NOT move routes out of main.py in this phase. The stubs satisfy the ABC contract and document intent. Route migration to webhook_router.py happens in Phase 20.

    After modifications, verify all three modules import cleanly: import ChannelAdapter from app.channels, import WhatsAppAdapter from app.channels.whatsapp, import TelegramAdapter from app.channels.telegram. All must succeed without runtime errors.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'services/nova-core'); from app.channels import ChannelAdapter; from app.channels.whatsapp import WhatsAppAdapter; from app.channels.telegram import TelegramAdapter; print('All imports OK')"</automated>
    <automated>python -c "import sys; sys.path.insert(0, 'services/nova-core'); from app.channels import ChannelAdapter; assert hasattr(ChannelAdapter, 'register_webhooks'); print('ABC has register_webhooks')"</automated>
    <automated>python -c "import sys; sys.path.insert(0, 'services/nova-core'); from app.channels.whatsapp import WhatsAppAdapter; assert hasattr(WhatsAppAdapter, 'register_webhooks'); print('WhatsAppAdapter has register_webhooks')"</automated>
    <automated>python -c "import sys; sys.path.insert(0, 'services/nova-core'); from app.channels.telegram import TelegramAdapter; assert hasattr(TelegramAdapter, 'register_webhooks'); print('TelegramAdapter has register_webhooks')"</automated>
  </verify>
  <done>
    - ChannelAdapter ABC has register_webhooks(self, app: FastAPI) abstract method
    - WhatsAppAdapter.register_webhooks exists as a no-op stub
    - TelegramAdapter.register_webhooks exists as a no-op stub
    - All modules import cleanly with no runtime errors
    - FastAPI imported only under TYPE_CHECKING — no new runtime dependency
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify SC compliance, update webhook_router skeleton, run WhatsApp test suite</name>
  <files>
    services/nova-core/app/channels/webhook_router.py (MODIFY)
  </files>
  <action>
    Three parts in sequence:

    Part A - Verify SC1 through SC4 schema compliance.
    Confirm all four schema-related SCs are satisfied by existing DDL (no additional migration needed beyond Task 1):
    SC1: Confirm user_preferences has last_active_channel TEXT DEFAULT 'whatsapp' and channels_enabled TEXT[] DEFAULT '{whatsapp}' — they are in 0001_initial_schema.py at the user_preferences table definition, columns last_active_channel with server_default "'whatsapp'" and channels_enabled with server_default "'{whatsapp}'".
    SC2: Confirm channel_identities has UNIQUE(channel, channel_id) — the unique index channel_identities_unique_idx is created in 0001_initial_schema.py on the channel_identities table.
    SC3: Task 1 drops the unused whatsapp_verification_codes table, leaving channel_verification_codes as the sole codes table which already has channel + channel_id columns for multi-channel use.
    SC4: Confirm queued_notifications has channel column (TEXT DEFAULT 'whatsapp') and nullable whatsapp_number (TEXT) — both defined in 0001_initial_schema.py queued_notifications table.

    Part B - Update webhook_router.py skeleton.
    Replace the file content entirely. The current file has a stale TODO referencing Phase 14. Replace with:
    Docstring: """Channel webhook registration and routing. Skeleton only. Phase 20 replaces with real webhook router that registers each channel adapter's webhook routes on the FastAPI app via the ChannelAdapter.register_webhooks() interface."""
    Followed by "from __future__ import annotations".
    Then a comment: "# TODO(Phase 20): Implement register_webhooks(app: FastAPI) that iterates over registered adapters and calls adapter.register_webhooks(app)."

    Part C - Run WhatsApp test suite for no-regression.
    First ensure pytest-asyncio is installed: "uv pip install pytest-asyncio" from the project root.
    Then run: python -m pytest services/nova-core/tests/test_webhooks.py -q --tb=short --no-header
    Then: python -m pytest services/nova-core/tests/test_outbound.py -q --tb=short --no-header
    Then: python -m pytest services/nova-core/tests/test_identity.py -q --tb=short --no-header

    Pre-existing known failures (NOT caused by Phase 19 changes): 5 tests in test_outbound.py patch "app.channels.whatsapp.is_user_in_dnd" but the actual symbol lives in app.identity. These were broken before any Phase 19 changes. All other WhatsApp-related tests (30+ in test_webhooks.py + test_identity.py + test_outbound.py) must pass.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'services/nova-core'); import subprocess; r = subprocess.run([sys.executable, '-m', 'pytest', 'services/nova-core/tests/test_webhooks.py', 'services/nova-core/tests/test_identity.py', '-q', '--tb=line', '--no-header'], capture_output=True, text=True); print(r.stdout[-600:]); assert r.returncode in (0, 1), f'Test run failed: {r.stderr}'"</automated>
  </verify>
  <done>
    - SC1 through SC4 schema elements confirmed in migration 0001
    - webhook_router.py references Phase 20, not Phase 14
    - pytest-asyncio is present in .venv
    - WhatsApp webhook, identity, and outbound tests pass (pre-existing outbound patching failures excluded)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alembic migration -> DB | DDL execution path; drop table operation must be reversible |
| ChannelAdapter ABC -> adapter implementations | Abstract method contract must be fulfilled by all subclasses |
| webhook registration (future) | Routes under /webhooks/* face untrusted external HTTP requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | Migration 0007 DDL | low | mitigate | Downgrade recreates table exactly — rollback is data-safe (table has zero rows). Review migration before applying to prod. |
| T-19-02 | Spoofing | ChannelAdapter ABC | low | accept | Abstract method is interface-only; no enforcement beyond Python's ABCMeta runtime check. Subclass conformance validated at import time. Phase 20 adds webhook_router which will use the interface. |
| T-19-SC | Tampering | pip installs (pytest-asyncio) | low | accept | Dev-only dependency; not deployed to production container. |</threat_model>

<verification>
- [ ] Migration 0007 drops whatsapp_verification_codes without affecting app code
- [ ] Alembic heads resolve to a single head after migration
- [ ] ChannelAdapter ABC has register_webhooks abstract method
- [ ] WhatsAppAdapter and TelegramAdapter implement register_webhooks
- [ ] All three modules import cleanly
- [ ] webhook_router.py TODO references Phase 20
- [ ] WhatsApp webhook tests pass (pre-existing outbound failures OK)
</verification>

<success_criteria>
- All 6 ROADMAP Success Criteria for Phase 19 are satisfied:
  SC1: user_preferences has last_active_channel + channels_enabled (verified, existed before this plan)
  SC2: channel_identities with UNIQUE(channel, channel_id) (verified, existed before this plan)
  SC3: channel_verification_codes generalizes WhatsApp-specific table (completed by migration 0007)
  SC4: queued_notifications has channel column + nullable whatsapp_number (verified, existed before this plan)
  SC5: WhatsAppAdapter conforms to ChannelAdapter interface (ABC now has register_webhooks, adapter implements it)
  SC6: channels/ package exists with ABC, InboundMessage, dispatcher.py skeleton, webhook_router.py skeleton (verified, existed before this plan)
- WhatsApp test suite shows no regression from Phase 19 changes
- All modified files committed
</success_criteria>

<output>
Create `.planning/phases/19-channel-adapter-pattern-multi-channel-schema/19-01-SUMMARY.md` when done
</output>
