---
phase: 01-agent-response-time-monitoring
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - services/nova-core/app/tracer.py
  - services/nova-core/app/agent_tracer.py
  - services/nova-core/app/main.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - AgentTrace dataclass carries per-iteration breakdown + UUID turn_id for Postgres FK linkage
    - Slowness beyond configured thresholds creates Forgejo issues with cooldown
    - Traces are insertable to Postgres via fire-and-forget agent_tracer functions
    - Dashboard can query recent traces via /dashboard/traces endpoint, filterable by user/channel
    - SSE stream yields named 'progress' events between regular poll cycles
  artifacts:
    - tracer.py with enriched AgentTrace, IterationTrace dataclass, slowness alerting
    - agent_tracer.py with insert_agent_turn() and insert_agent_iterations()
    - main.py with /dashboard/traces GET and SSE progress event yield
  key_links:
    - tracer.py: slowness check reads settings.nova_slow_* thresholds (from Plan 01)
    - agent_tracer.py: inserts into agent_turns / agent_iterations tables (from Plan 01)
    - main.py: dashboard_stream() reads progress queue (from Plan 01)
---

<objective>
Build the storage, alerting, and dashboard layers: enriched tracer with slowness alerts, fire-and-forget Postgres writer, and dashboard endpoints for trace queries + live SSE progress.

**Purpose:** Wire the three output paths for per-iteration trace data — OpenObserve (enriched tracer), Postgres (normalized inserts), and dashboard (REST query + SSE live feed). Add slowness detection that creates Forgejo issues.

**Output:**
- `tracer.py` — enriched `AgentTrace` + `IterationTrace` dataclass + `_file_slow_alert()` Forgejo issue creation
- `agent_tracer.py` — new module with `insert_agent_turn()` + `insert_agent_iterations()` functions
- `main.py` — `/dashboard/traces` endpoint + `dashboard_stream()` progress event integration
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-add-a-new-phase-to-monitor-the-response-time-and-prompts-for/01-CONTEXT.md
@.planning/phases/01-add-a-new-phase-to-monitor-the-response-time-and-prompts-for/01-RESEARCH.md
@services/nova-core/app/tracer.py
@services/nova-core/app/config.py
@services/nova-core/app/forgejo.py
@services/nova-core/app/feedback.py
@services/nova-core/app/main.py
@services/nova-core/app/db.py
@services/nova-core/tests/test_tracer.py
@services/nova-core/tests/test_dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich AgentTrace dataclass and add Forgejo slowness alerting to tracer.py</name>
  <files>services/nova-core/app/tracer.py</files>
  <action>
    Modify `services/nova-core/app/tracer.py` to:
    1. Add an `IterationTrace` dataclass (before `AgentTrace`) with fields per D-09: `iteration_num: int`, `llm_time_ms: int`, `tool_time_ms: int`, `tool_name: str`, `prompt_tokens: int = 0`, `completion_tokens: int = 0`. Follow existing @dataclass style from `AgentTrace`.
    2. Add two new fields to `AgentTrace`: `turn_id: str = ""` (UUID string for Postgres FK linkage per D-09) and `iterations: list[dict] = field(default_factory=list)` (per-iteration breakdown per D-01). Preserve all existing fields.
    3. Add a `_file_slow_alert()` module-level async function that:
       - Reads `settings.nova_slow_llm_ms`, `nova_slow_tool_ms`, `nova_slow_turn_ms` from config (per D-08)
       - Iterates over `trace.iterations` — if any iteration's `llm_time_ms > settings.nova_slow_llm_ms` OR `tool_time_ms > settings.nova_slow_tool_ms`, OR `trace.latency_ms > settings.nova_slow_turn_ms`, file a Forgejo issue (per D-06, D-07)
       - Uses a module-level `_last_slow_alert: dict[str, float]` for cooldown (default 300s between same-type alerts) — follows RESEARCH Pitfall 4 mitigation
       - Creates `ForgejoClient(settings.forgejo_url, settings.forgejo_repo, settings.forgejo_token)` and calls `create_issue(title, body, labels=["slowness"])` — exactly matching the pattern in `feedback.py:251-272`
       - Skips silently if `settings.forgejo_token` is unset (same pattern as `feedback.py:255-257`)
       - Logs with %s-style lazy formatting (not f-strings) per codebase conventions
    4. Add an `async def check_and_alert_slowness(trace: AgentTrace) -> None` public function that calls `_file_slow_alert()` for each threshold type exceeded. This will be called from `agent.py` after turn completion.
    5. Update the module docstring to reflect the new responsibilities.
    6. Add test coverage in existing `test_tracer.py`:
       - `test_iteration_trace_dataclass_fields` — verifies all IterationTrace fields
       - `test_agent_trace_iterations_field` — verifies enriched AgentTrace has iterations + turn_id
       - `test_slowness_check_creates_forgejo_issue` — mocks ForgejoClient, calls check_and_alert_slowness with a trace that has llm_time_ms > threshold, verifies create_issue was called
       - `test_slowness_check_skips_within_cooldown` — verifies cooldown suppresses duplicates
       - `test_slowness_check_skips_when_not_configured` — verifies silent skip when forgejo_token unset

    Do NOT change `emit_trace()` signature — it continues accepting `AgentTrace` (now enriched with the new fields). Do NOT change how `emit_trace` serializes — `asdict(trace)` will automatically include the new `iterations` and `turn_id` fields in the OpenObserve payload (per D-11).

    Do NOT use f-string logging — use %s-style lazy formatting for all new log statements.
  </action>
  <verify>
    <automated>cd services/nova-core && python -m pytest tests/test_tracer.py -x -q --timeout=30 -k "test_iteration_trace_dataclass_fields or test_agent_trace_iterations_field or test_slowness_check_skips_when_not_configured"</automated>
  </verify>
  <done>tracer.py has IterationTrace dataclass, enriched AgentTrace with iterations+turn_id, check_and_alert_slowness() function that creates Forgejo issues with cooldown; all tracer tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create agent_tracer.py for fire-and-forget Postgres trace inserts</name>
  <files>services/nova-core/app/agent_tracer.py</files>
  <action>
    Create `services/nova-core/app/agent_tracer.py` — fire-and-forget Postgres writer for per-iteration traces (per D-03, D-09).

    Follow codebase conventions:
    - Module docstring explaining role ("Fire-and-forget Postgres writer for agent turn + iteration traces").
    - `from __future__ import annotations`
    - `%s`-style lazy logging
    - Internal `try/except` wrapping all DB operations (never raise to caller — same pattern as `emit_trace()`)

    Contents:
    1. `async def insert_agent_turn(trace: dict) -> None`
       - Acquires pool via `await db.get_pool()`
       - Uses `conn.execute()` with parameterized `$1` queries (per existing pattern in `main.py:224-233`, `audit.py`)
       - Inserts into `agent_turns`: `user, channel, total_latency_ms, token_count, iteration_count, got_stuck, error_count`
       - Returns `None` on success, logs warning on failure (never raises)
       - Per D-03: fire-and-forget — expected to be called via `asyncio.create_task()`

    2. `async def insert_agent_iterations(turn_id: str, iterations: list[dict]) -> None`
       - Takes `turn_id` (UUID string) and `iterations` (list of dicts with iteration_num, llm_time_ms, tool_time_ms, tool_name, prompt_tokens, completion_tokens)
       - Acquires pool, uses `conn.executemany()` for batch insert
       - Returns `None`, logs warning on failure (never raises)
       - Follows same parameterized query pattern

    3. `async def insert_agent_traces(trace: AgentTrace) -> None` — convenience wrapper that:
       - Calls `insert_agent_turn(turn_data)` and `insert_agent_iterations(trace.turn_id, trace.iterations)` sequentially in the same connection
       - Both wrapped in single try/except so a failure in iterations doesn't orphan a turn
       - Uses `asyncio.gather()` ... NO — keep sequential (turn must exist before iterations for FK)

    4. Import `AgentTrace` from `tracer.py` for type hint.

    All DB write errors are logged at warning level and silently absorbed — never propagate to the caller.

    Do NOT add any FastAPI imports or HTTP logic — this is a pure DB writer module.

    Add test coverage in a new or existing test file:
    - Create `test_agent_tracer.py` with tests that mock the asyncpg pool and verify SQL was called with correct parameters
    - Test that both insert functions handle DB errors gracefully (log warning, no exception)
  </action>
  <verify>
    <automated>cd services/nova-core && python -m pytest tests/test_agent_tracer.py -x -q --timeout=30</automated>
  </verify>
  <done>agent_tracer.py exists with insert_agent_turn(), insert_agent_iterations(), insert_agent_traces() — all fire-and-forget, errors logged but never raised; test_agent_tracer.py passes</done>
</task>

<task type="auto">
  <name>Task 3: Add /dashboard/traces endpoint and SSE progress integration to main.py</name>
  <files>services/nova-core/app/main.py</files>
  <action>
    Modify `services/nova-core/app/main.py` with two additions:

    **A) `/dashboard/traces` GET endpoint** (per D-10):
    - Add after the existing `/dashboard/audit` endpoint (around line 345)
    - Query params: `user` (optional, Text), `channel` (optional, Text), `limit` (int, default 20)
    - Query `agent_turns` table with LEFT JOIN to `agent_iterations`, ordered by `created_at DESC`
    - Filter by `user` and `channel` if provided (parameterized `$1`, `$2` — never string interpolation)
    - Return shape: `{"traces": [{"id", "user", "channel", "total_latency_ms", "token_count", "iteration_count", "got_stuck", "error_count", "created_at", "iterations": [...]}]}`
    - Each trace's iterations are aggregated via a second query (fetch all iterations for those turn IDs) and merged in Python — follow the same pattern as `dashboard_task_detail()` which does sequential queries
    - Follow existing endpoint patterns (`@app.get(...)`, `async def`, `pool = await db.get_pool()`, parameterized queries with `$1`)
    - Do NOT add auth — inherits existing LAN-only access model (same as `/dashboard/tasks`, `/dashboard/audit`)
    - Import needed: no new imports needed (already has `from . import admin_models, llm, db`)

    **B) SSE progress event integration in `dashboard_stream()`** (per D-04):
    - Add import at top: `from .progress import get_progress_queue`
    - Inside the `event_generator()` function, before the `asyncio.sleep(15)` call (around line 543), drain the progress queue using `get_progress_queue()`:
      ```python
      try:
          while True:
              progress = _progress_queue.get_nowait()
              yield f"event: progress\ndata: {json.dumps(progress)}\n\n"
      except asyncio.QueueEmpty:
          pass
      ```
    - Use `get_progress_queue()` to obtain the queue (not direct module-level access) — follows the accessor pattern from `progress.py`
    - The `event: progress` named SSE event matches the existing `event: status` pattern in `/admin/stream` at line 945

    Add tests in existing `test_dashboard.py`:
    - `test_dashboard_traces_endpoint` — mock DB pool, verify correct SQL called and response shape
    - Import `TestClient` from `fastapi.testclient` (already used in `test_dashboard.py`)

    Do NOT use f-string logging — use %s-style lazy formatting.
  </action>
  <verify>
    <automated>cd services/nova-core && python -m pytest tests/test_dashboard.py -x -q --timeout=30 -k "test_dashboard_traces"</automated>
  </verify>
  <done>main.py has /dashboard/traces endpoint returning traces with iterations, filterable by user/channel; dashboard_stream() yields named progress events from the queue</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent loop → Forgejo API | Untrusted external API (git.7rb.nl) — token auth, rate limiting |
| dashboard endpoint → client | LAN-only access — no auth added per existing pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | /dashboard/traces endpoint | low | accept | Exposes household names (user column) + timing data — LAN-only, same access model as existing dashboard endpoints |
| T-02-02 | Tampering | Forgejo issue content | low | mitigate | Issue body is structured Markdown with truncated fields (same pattern as feedback.py). Tool arg values are NOT included — only tool_name |
| T-02-03 | DoS | Forgejo alert spam | low | mitigate | Module-level cooldown dict (300s per alert type) — follows Research Pitfall 4 |
| T-02-04 | Tampering | agent_tracer SQL injection | low | mitigate | Parameterized $1 queries via asyncpg — no string interpolation |
| T-02-05 | Repudiation | SSE progress events dropped | low | accept | Queue bound at maxsize=100; drops on overflow — progress events are ephemeral by nature |
</threat_model>

<verification>
1. `cd services/nova-core && python -m pytest tests/test_tracer.py -x -q --timeout=30` — tracer enrichment tests pass
2. `cd services/nova-core && python -m pytest tests/test_agent_tracer.py -x -q --timeout=30` — agent_tracer tests pass
3. `cd services/nova-core && python -m pytest tests/test_dashboard.py -x -q --timeout=30 -k "test_dashboard_traces"` — traces endpoint test passes
4. `cd services/nova-core && python -m pytest tests/ -x -q --timeout=30` — all existing tests still pass
</verification>

<success_criteria>
- tracer.py has IterationTrace + enriched AgentTrace + check_and_alert_slowness() with cooldown
- agent_tracer.py has insert_agent_turn(), insert_agent_iterations(), insert_agent_traces() — all fire-and-forget
- main.py has /dashboard/traces endpoint (filterable by user/channel) and SSE progress event integration
- All existing tests plus new tracer/agent_tracer/dashboard tests pass
</success_criteria>

<output>
Create `.planning/phases/01-add-a-new-phase-to-monitor-the-response-time-and-prompts-for/01-02-SUMMARY.md` when done
</output>