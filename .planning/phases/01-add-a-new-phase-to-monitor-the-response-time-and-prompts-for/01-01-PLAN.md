---
phase: 01-agent-response-time-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/nova-core/app/config.py
  - services/nova-core/app/progress.py
  - services/nova-core/alembic/versions/0016_agent_traces.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - Agent loop can report per-iteration timing breakdown (LLM call + tool execution)
    - Live progress events can flow from agent loop to dashboard SSE stream
    - Traces are persisted in both OpenObserve (enriched payload) and Postgres (normalized schema)
    - Slowness thresholds are configurable via env vars without code changes
    - Operator gets notified when individual steps exceed aggressive thresholds
  artifacts:
    - config.py with nova_slow_llm_ms, nova_slow_tool_ms, nova_slow_turn_ms fields
    - progress.py with asyncio.Queue and push_progress() function
    - 0016_agent_traces.py migration with agent_turns + agent_iterations tables
  key_links:
    - config.py threshold fields consumed by tracer.py (threshold checks)
    - progress.py queue consumed by agent.py (writer) and main.py (SSE reader)
    - agent_turns/agent_iterations tables consumed by agent_tracer.py (inserts) and main.py (dashboard endpoint)
---

<objective>
Build the foundation layer for agent response time monitoring: configurable thresholds, progress event queue, and normalized storage schema.

**Purpose:** Establish the three infrastructure pillars that downstream plans wire up — env-var thresholds (D-06, D-08), an async progress queue for SSE events (D-04, D-05), and Postgres tables for structured trace storage (D-09).

**Output:**
- `config.py` — 3 new threshold fields on `Settings`
- `progress.py` — new module with `asyncio.Queue` + `push_progress()`  
- `0016_agent_traces.py` — new Alembic migration creating `agent_turns` + `agent_iterations`
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-add-a-new-phase-to-monitor-the-response-time-and-prompts-for/01-CONTEXT.md
@.planning/phases/01-add-a-new-phase-to-monitor-the-response-time-and-prompts-for/01-RESEARCH.md
@services/nova-core/app/config.py
@services/nova-core/alembic/versions/0015_task_notes_and_template.py
@services/nova-core/app/tracer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add slowness threshold env vars to config.py</name>
  <files>services/nova-core/app/config.py</files>
  <action>
    Add three new fields to the `Settings` class in config.py (per D-06, D-08). Place them after `nova_tracing_enabled: bool = True` (line 20), grouped under a comment `# Tracing / slowness thresholds`. Follow the existing `nova_*` naming convention and pydantic-settings env_file=".env" pattern. Use `int` type — all values in milliseconds.

    Fields to add (per D-06 defaults and CONTEXT.md specific ideas):
    - `nova_slow_llm_ms: int = 15000` — LLM inference call exceeds this → slowness alert
    - `nova_slow_tool_ms: int = 5000` — Single tool execution exceeds this → slowness alert
    - `nova_slow_turn_ms: int = 30000` — Total agent turn exceeds this → slowness alert

    Follow existing docstring style from adjacent fields. Do NOT modify any existing fields or imports.

    Do NOT use f-string logging in any new code — use %s-style lazy formatting to match codebase conventions (per CLAUDE.md §Logging).
  </action>
  <verify>
    <automated>cd services/nova-core && python -c "from app.config import settings; print(settings.nova_slow_llm_ms, settings.nova_slow_tool_ms, settings.nova_slow_turn_ms)"</automated>
  </verify>
  <done>Three nova_slow_* fields present on Settings class with correct defaults (15000, 5000, 30000), accessible as pydantic-settings attributes</done>
</task>

<task type="auto">
  <name>Task 2: Create progress.py module with asyncio.Queue progress bus</name>
  <files>services/nova-core/app/progress.py</files>
  <action>
    Create `services/nova-core/app/progress.py` — a shared async progress-event bus for the dashboard SSE stream (per D-04, D-05).

    Follow codebase conventions from `conventions.md`:
    - Module docstring explaining role
    - `from __future__ import annotations` as first import
    - `snake_case` function names
    - `UPPER_SNAKE_CASE` for module-level constants
    - `%s`-style lazy logging, not f-strings (per CLAUDE.md §Logging)

    Contents:
    1. Module-level `_progress_queue: asyncio.Queue[dict]` with `maxsize=100` (per RESEARCH Pitfall 2 — bounded to prevent memory exhaustion under high load)
    2. `async def push_progress(step: str, elapsed_s: float) -> None` — non-blocking push using `put_nowait()` that silently drops on `asyncio.QueueFull` (progress events are ephemeral — losing one is acceptable). The payload dict shape per D-05: `{"step": step, "elapsed_s": elapsed_s}`
    3. Module-level `get_progress_queue() -> asyncio.Queue[dict]` function that returns `_progress_queue` — used by `main.py`'s SSE generator to drain the queue. Do NOT export `_progress_queue` directly; use the accessor function.

    Do NOT use f-string logging — use %s-style: `log.warning("progress queue full, dropping event: step=%s", step)`

    This module is imported by `agent.py` (writer) and `main.py` (reader). Keep it import-order safe — no circular imports.
  </action>
  <verify>
    <automated>cd services/nova-core && python -c "from app.progress import push_progress, get_progress_queue; import asyncio; q = get_progress_queue(); asyncio.run(push_progress('test', 1.0))"</automated>
  </verify>
  <done>progress.py exists with asyncio.Queue(maxsize=100), push_progress() non-blocking pusher, get_progress_queue() accessor</done>
</task>

<task type="auto">
  <name>Task 3: Create Alembic migration 0016 for agent_turns + agent_iterations</name>
  <files>services/nova-core/alembic/versions/0016_agent_traces.py</files>
  <action>
    Create `services/nova-core/alembic/versions/0016_agent_traces.py` — a new Alembic migration that creates two normalized tables for per-iteration trace storage (per D-09).

    Follow the exact pattern from `0014_planned_blocks.py` and `0015_task_notes_and_template.py`:
    - Imports: `from typing import Sequence, Union`, `from alembic import op`, `import sqlalchemy as sa`
    - `revision: str = "0016"`, `down_revision: Union[str, None] = "0015"`
    - `branch_labels = None`, `depends_on = None`
    - Docstring at top: `"""Create agent_turns and agent_iterations tables (Phase 1 — response time monitoring)"""`

    **agent_turns table** (turn-level):
    - `id` — UUID, PK, `server_default=sa.text("uuid_generate_v4()")`, nullable=False
    - `user` — Text, nullable=False (household name, per D-09)
    - `channel` — Text, nullable=False (api/whatsapp/telegram/voice)
    - `total_latency_ms` — Integer, nullable=False
    - `token_count` — Integer, nullable=False
    - `iteration_count` — Integer, nullable=False
    - `got_stuck` — Boolean, server_default=false, nullable=False
    - `error_count` — Integer, server_default=0, nullable=False
    - `created_at` — DateTime(timezone=True), server_default=now(), nullable=False
    - PK on `id`
    - Index on `created_at`
    - Index on `(user, channel)` for dashboard filtering per D-10

    **agent_iterations table** (per-iteration):
    - `id` — UUID, PK, `server_default=sa.text("uuid_generate_v4()")`, nullable=False
    - `turn_id` — UUID, FK → agent_turns.id ON DELETE CASCADE, nullable=False
    - `iteration_num` — Integer, nullable=False
    - `llm_time_ms` — Integer, nullable=False
    - `tool_time_ms` — Integer, nullable=True (None if no tool was called in this iteration)
    - `tool_name` — Text, nullable=True
    - `prompt_tokens` — Integer, server_default=0, nullable=False
    - `completion_tokens` — Integer, server_default=0, nullable=False
    - `created_at` — DateTime(timezone=True), server_default=now(), nullable=False
    - FK (turn_id) → agent_turns.id ON DELETE CASCADE
    - Index on turn_id

    downgrade() drops indexes then tables in reverse order (iterations first, then turns).

    Do NOT modify existing migrations. Do NOT import any app modules. Use only Alembic/SQLAlchemy primitives.
  </action>
  <verify>
    <automated>cd services/nova-core && python -c "from alembic.config import Config; from alembic import command; cfg = Config('alembic.ini'); command.check(cfg)"</automated>
  </verify>
  <done>0016_agent_traces.py creates both tables with correct schema, FKs, indexes; downgrade reverses cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent loop → progress queue | In-process asyncio.Queue — same process, no trust boundary |
| agent loop → Postgres | Fire-and-forget asyncpg writes via existing pool — LAN-only DB access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | DoS | asyncio.Queue (progress.py) | low | mitigate | maxsize=100 with put_nowait() — drops events on overflow (Research Pitfall 2) |
| T-01-02 | Information Disclosure | agent_turns.user column | low | accept | Stores household name only (not PII); /dashboard/traces inherits existing LAN-only access (same as other dashboard endpoints) |
| T-01-03 | Tampering | Migration DDL | low | mitigate | Schema changes use Alembic parameterized operations (no raw SQL injection) |
</threat_model>

<verification>
1. `cd services/nova-core && python -c "from app.config import settings; print(settings.nova_slow_llm_ms)"` returns 15000
2. `cd services/nova-core && python -c "from app.progress import push_progress, get_progress_queue; import asyncio; q = get_progress_queue(); asyncio.run(push_progress('test', 1.0))"` succeeds without error
3. `cd services/nova-core && python -c "from alembic.config import Config; from alembic import command; cfg = Config('alembic.ini'); command.check(cfg)"` passes
4. `pytest services/nova-core/tests/ -x -q --timeout=30` (existing suite still passes)
</verification>

<success_criteria>
- Three nova_slow_* fields on Settings with correct defaults (15000, 5000, 30000)
- progress.py with bounded asyncio.Queue and non-blocking push_progress() that drops on overflow
- 0016_agent_traces.py migration creates agent_turns + agent_iterations per D-09 schema
- All existing tests still pass
</success_criteria>

<output>
Create `.planning/phases/01-add-a-new-phase-to-monitor-the-response-time-and-prompts-for/01-01-SUMMARY.md` when done
</output>
