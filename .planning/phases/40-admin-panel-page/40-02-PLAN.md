---
phase: 40-admin-panel-page
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - services/nova-core/static/admin.html
  - services/nova-core/static/admin.js
  - services/nova-core/static/style.css
  - services/nova-core/tests/test_admin.py
autonomous: true
requirements:
  - D-02
  - D-03
  - D-06
  - D-07
  - D-09
  - D-10
user_setup: []

must_haves:
  truths:
    - "GET /static/admin.html returns 200 and serves the admin page HTML (D-06)"
    - "admin.html reuses style.css design tokens (glass-panel, pulse-dot, status-indicator, dashboard-container) — no new design language (D-06)"
    - "admin.html contains #system-status-panel with 5 service cells (Ollama, Postgres, CalDAV, HA, Email) and #channel-status-panel with Ruben/Méral tab selector (UI-SPEC §Page Structure)"
    - "admin.js opens EventSource('/admin/stream') and listens for the named `status` event (D-10)"
    - "On SSE error, admin.js shows a page-level banner with `Admin stream disconnected. Retrying…` — NOT a modal/alert (UI-SPEC §SSE/Refresh)"
    - "On unchanged cell status, admin.js preserves the existing DOM (no flash/re-animation); only changed cells animate via existing fadeIn keyframe (UI-SPEC §SSE/Refresh)"
    - "index.html is NOT modified — no /admin link appears on the household dashboard (D-07, D-09)"
    - "admin.html includes a real `<a href='/'>` Back-to-Dashboard anchor (keyboard-focusable, works without JS) per UI-SPEC §Accessibility"
    - "All dynamic strings rendered by admin.js go through escapeHtml — no innerHTML with untrusted text"
  artifacts:
    - "services/nova-core/static/admin.html exists with the two-card layout + Back-to-Dashboard link"
    - "services/nova-core/static/admin.js exists with EventSource consumer + diffing renderer + escapeHtml helper"
    - "services/nova-core/static/style.css contains an admin-specific block (.admin-status-cell grid, .admin-service-grid, channel-status cell border-left fade for unlinked)"
    - "services/nova-core/tests/test_admin.py gains test_admin_html_served and test_admin_html_structure (passing)"
  key_links:
    - "admin.js EventSource('/admin/stream') consumes the named-event payload shape locked by Plan 01's tests"
    - "admin.html references /static/style.css and /static/admin.js (served by the existing StaticFiles mount at main.py:976-977)"
    - "admin.js addEventListener('status', cb) — distinct from dashboard's onmessage pattern (UI-SPEC §SSE/Refresh)"
    - "admin.html header uses the same .logo-area + .status-indicator + .pulse-dot pattern as index.html"
---

<objective>
Build the frontend of the read-only admin status board: `admin.html` (glass-panel layout with two full-width cards — System Status 5-cell grid + Channel Status per-user tab selector), `admin.js` (EventSource consumer with named-event listener, DOM-diffing renderer that avoids re-animating unchanged cells, escapeHtml helper, page-level error banner on SSE failure), and a small admin-specific block in `style.css`. Add the two frontend structure tests to `test_admin.py` (created by Plan 01). Do NOT touch `index.html` (D-07 gate).

Purpose: Render the SSE payload from `/admin/stream` into a status board that visually matches the household dashboard. Per D-04 and D-06, this is read-only and shares the existing CSS design language — no new component library, no new fonts, no new colors.

Output:
- New `services/nova-core/static/admin.html` — page structure
- New `services/nova-core/static/admin.js` — SSE consumer + renderer
- Modified `services/nova-core/static/style.css` — admin-specific cells + grid (append-only)
- Modified `services/nova-core/tests/test_admin.py` — append `test_admin_html_served` and `test_admin_html_structure`
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-admin-panel-page/40-CONTEXT.md
@.planning/phases/40-admin-panel-page/40-RESEARCH.md
@.planning/phases/40-admin-panel-page/40-UI-SPEC.md
@.planning/phases/40-admin-panel-page/40-01-SUMMARY.md
@services/nova-core/static/index.html
@services/nova-core/static/app.js
@services/nova-core/static/style.css
@services/nova-core/tests/test_admin.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create admin.html + style.css admin block + frontend structure tests</name>
  <files>
    services/nova-core/static/admin.html,
    services/nova-core/static/style.css,
    services/nova-core/tests/test_admin.py
  </files>
  <read_first>
    - services/nova-core/static/index.html (full — clone header + container structure)
    - services/nova-core/static/style.css (full — to find glass-panel, dashboard-card, pulse-dot, status-indicator, chat-user-tab, assignee-section, audit-panel grid-column: span 2 patterns)
    - services/nova-core/tests/test_admin.py (Plan 01's file — append the two frontend tests)
    - services/nova-core/tests/test_dashboard.py (lines 128-141 — Clone test_dashboard_html_has_chat_panel pattern)
    - .planning/phases/40-admin-panel-page/40-UI-SPEC.md (full — §Page Structure, §Service Status Cell, §Channel Status Cell, §Copywriting Contract, §Accessibility)
  </read_first>
  <behavior>
    - test_admin_html_served: GET /static/admin.html returns 200
    - test_admin_html_structure: response body contains `id="system-status-panel"`, `id="channel-status-panel"`, `id="service-ollama"`, `id="service-postgres"`, `id="service-caldav"`, `id="service-ha"`, `id="service-email"`, `id="channel-ruben"`, `id="channel-meral"`, the Back-to-Dashboard anchor `<a href="/"`, and the script tag `<script src="/static/admin.js"></script>`
    - test_admin_html_no_admin_link_on_dashboard: GET /static/index.html does NOT contain any string containing `/admin` (proves D-07 from the frontend side — Plan 01's git-diff gate proves it from the source side)
  </behavior>
  <action>
**1. Append to `services/nova-core/tests/test_admin.py`** the two new tests (test_admin_html_served, test_admin_html_structure, test_admin_html_no_admin_link_on_dashboard). Mirror test_dashboard_html_has_chat_panel (test_dashboard.py:128-132). For test_admin_html_structure, assert each of the IDs above is present as a byte substring in the response content. For test_admin_html_no_admin_link_on_dashboard: `resp = client.get("/static/index.html"); assert b"/admin" not in resp.content` (this is a smoke test on the served file, not a git diff — both gates are valuable). Run `python -m pytest tests/test_admin.py::test_admin_html_served tests/test_admin.py::test_admin_html_structure tests/test_admin.py::test_admin_html_no_admin_link_on_dashboard -x` — these MUST FAIL (RED) because `admin.html` does not yet exist. Commit RED with `test(40-02): add failing admin frontend structure tests`.

**2. Create `services/nova-core/static/admin.html`** by cloning the structural skeleton of `index.html` (the `<head>` with Google Fonts links, `<link rel="stylesheet" href="/static/style.css">`, the `<body>` with `<div class="dashboard-container">`, the `<div class="dashboard-header">` with `.logo-area` and `.status-indicator`). Per UI-SPEC §Page Structure:

- `<title>Nova Admin</title>`
- Header `.logo-area h1`: `Nova <span>Admin</span>` (the span uses `--accent-color` — reuse the existing dashboard pattern where `Nova <span>Household</span>` is styled)
- Header `.status-indicator`: `<span class="pulse-dot"></span> Live Connection` (clone from index.html)
- Header right-side `.time-area`: real anchor `<a href="/" class="btn btn-secondary" id="back-to-dashboard">← Back to Dashboard</a>` — a real `<a>` not a JS click handler (UI-SPEC §Accessibility); NO settings cog (admin is read-only)
- Page-level error banner: `<div class="chat-error" id="admin-error-banner" hidden>Admin stream disconnected. Retrying…</div>` — initially hidden, surfaced by admin.js on SSE error. Reuse the existing `.chat-error` class.
- Main `.dashboard-grid`: two cards stacked:
  - Top card `<section class="dashboard-card glass-panel audit-panel" id="system-status-panel" style="grid-column: span 2;">` (reuse `.audit-panel` for the span-2 pattern; class names from style.css:706-715):
    - `<div class="card-header"><h2>System Status</h2></div>`
    - `<div class="admin-service-grid">` containing 5 cells: `<div class="admin-service-cell" id="service-ollama" data-status="" aria-label="Ollama: Checking…">` … each cell has children: `<div class="admin-service-name">Ollama</div>`, `<span class="pulse-dot" aria-hidden="true"></span>`, `<div class="admin-service-status">Checking Ollama…</div>`, `<div class="admin-service-detail">—</div>` (placeholder copy per UI-SPEC §Copywriting Loading state). Repeat for Postgres, CalDAV, Home Assistant, Email (IMAP). Cell IDs: `service-ollama`, `service-postgres`, `service-caldav`, `service-ha`, `service-email`. aria-label format: `"{Service Name}: {status}"` (UI-SPEC §Accessibility).
  - Bottom card `<section class="dashboard-card glass-panel audit-panel" id="channel-status-panel" style="grid-column: span 2;">`:
    - `<div class="card-header"><h2>Channel Status</h2></div>`
    - `<div class="chat-user-tabs">` with two `<button class="chat-user-tab" data-user="Ruben">Ruben</button>` and `<button class="chat-user-tab" data-user="Meral">Méral</button>` — NO "Household" tab (UI-SPEC §Page Structure; channel linking is per-user). The first tab (`Ruben`) gets `class="chat-user-tab active"` and the second gets the class without `active`.
    - `<div class="admin-channel-grid">` containing two cells: WhatsApp (`id="channel-ruben-whatsapp"`, `data-user="Ruben"`, `data-channel="whatsapp"`) + Telegram (`id="channel-ruben-telegram"`, `data-user="Ruben"`, `data-channel="telegram"`). Per UI-SPEC §Channel Status Cell: each cell is `assignee-section`-style — border-left 3px accent when linked, faded to `--text-secondary` when unlinked. Add a WhatsApp cell and Telegram cell for Meral too (initially hidden via CSS or rendered invisibly; admin.js toggles visibility based on active tab). Simplest: render all 4 cells (Ruben/whatsapp, Ruben/telegram, Meral/whatsapp, Meral/telegram) and use `[data-user]` selector + tab toggle to show only the active user's pair.
- Bottom: `<script src="/static/admin.js"></script>` (NOT app.js — D-06). No `app.js` import on admin.html.
- Use real `<a href="/">` for Back-to-Dashboard (UI-SPEC §Accessibility — works without JS).
- Use `aria-label` on each `.admin-service-cell` parent (status dot color alone is not adequate per UI-SPEC §Accessibility).
- `aria-hidden="true"` on each `<span class="pulse-dot">`.

**3. Append to `services/nova-core/static/style.css`** a small admin-specific block (do NOT modify existing rules; append-only). Add:
- `.admin-service-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; }` with `@media (max-width: 1200px) { .admin-service-grid { grid-template-columns: repeat(2, 1fr); } }` and `@media (max-width: 800px) { .admin-service-grid { grid-template-columns: 1fr; } }` (UI-SPEC §Page Structure: 3 cols wide, 2 below 1200px, 1 below 800px)
- `.admin-service-cell { background: rgba(255, 255, 255, 0.02); border: 1px solid rgba(255, 255, 255, 0.05); border-radius: 12px; padding: 1rem; display: flex; flex-direction: column; gap: 0.25rem; }` (matches `.assignee-section` / `.event-item` styling per UI-SPEC §Service Status Cell)
- `.admin-service-name { font-size: 0.95rem; font-weight: 600; }` (Body role per UI-SPEC §Typography — do NOT introduce a fifth size or third weight)
- `.admin-service-status { font-size: 0.8rem; font-weight: 600; line-height: 1.3; }` (Label/meta role)
- `.admin-service-status.ok { color: var(--success-color); } .admin-service-status.down { color: var(--warning-color); }`
- `.admin-service-detail { font-size: 0.8rem; color: var(--text-secondary); line-height: 1.3; }`
- Status dot color helpers: `.admin-service-cell[data-status="ok"] .pulse-dot { background: var(--success-color); animation: pulse 2s ease-in-out infinite; } .admin-service-cell[data-status="down"] .pulse-dot { background: var(--warning-color); animation: none; }` — UI-SPEC §Copywriting locks: "pulse is reserved for healthy state only." The red dot is a static circle. Reuse the existing `@keyframes pulse` defined in style.css (do NOT redefine it).
- `.admin-channel-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem; }`
- `.admin-channel-cell { background: rgba(255, 255, 255, 0.02); border: 1px solid rgba(255, 255, 255, 0.05); border-left: 3px solid var(--accent-color); border-radius: 12px; padding: 1rem; display: flex; flex-direction: column; gap: 0.5rem; }` (border-left 3px accent per UI-SPEC §Channel Status Cell)
- `.admin-channel-cell.unlinked { border-left-color: var(--text-secondary); }` (fades to secondary when unlinked — mirrors `.assignee-section.household` pattern)
- `.admin-channel-status { font-size: 0.95rem; } .admin-channel-status .status-value { color: var(--text-secondary); }`
- `.admin-channel-cell.linked .admin-channel-status .status-value { color: var(--success-color); }`
- A rule to hide inactive-tab channel cells: `.admin-channel-cell[data-user]:not([data-user="Ruben"]) { display: none; }` — but this is overridden by admin.js adding an `active-tab` class to the container. Simplest: do NOT bake the tab-toggling into CSS; admin.js manages `hidden` attribute on each cell based on active tab. CSS only styles the cells; admin.js owns visibility. So skip this rule and let admin.js handle `[hidden]` (HTML's hidden attribute already displays:none in modern browsers).
- For `.chat-user-tabs` reuse the existing `.chat-user-tab` styles from index.html (do NOT duplicate — the existing CSS class already styles the buttons). Only add `.chat-user-tab.active` if style.css doesn't already define it.

Run `cd services/nova-core && python -m pytest tests/test_admin.py -x` — all tests must pass GREEN (the two new tests + all the Plan 01 backend tests still pass). Commit with `feat(40-02): add admin.html + style.css admin block + structure tests`.

Do NOT include `<script src="/static/app.js">` on admin.html (D-06 — admin logic stays in admin.js).
Do NOT add a `/admin` link to `index.html` (D-07 gate; index.html must remain unchanged in this plan's diff).
Do NOT introduce a 5th type size or 3rd font weight — reuse the existing 4-size, 2-weight system (UI-SPEC §Typography).
Do NOT introduce new CSS color variables — reuse `--bg-color`, `--panel-bg`, `--accent-color`, `--warning-color`, `--success-color`, `--text-secondary` (UI-SPEC §Color).
  </action>
  <verify>
    <automated>cd services/nova-core && python -m pytest tests/test_admin.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `admin.html` exists and is served at `/static/admin.html` with status 200.
    - `admin.html` is served with a `<link rel="stylesheet" href="/static/style.css">` reference and a `<script src="/static/admin.js">` reference (NOT `app.js`).
    - `admin.html` contains `id="system-status-panel"` (top card), `id="channel-status-panel"` (bottom card), and the 5 per-service cell IDs (`service-ollama`, `service-postgres`, `service-caldav`, `service-ha`, `service-email`) plus per-user channel cell IDs (`channel-ruben-whatsapp`, `channel-ruben-telegram`, `channel-meral-whatsapp`, `channel-meral-telegram` — or equivalent `[data-user]` pattern).
    - `admin.html` contains `<a href="/" ...>← Back to Dashboard</a>` — real anchor, not a click handler.
    - `admin.html` does NOT reference `/static/app.js`.
    - `style.css` diff is append-only (no existing rules modified).
    - `index.html` is NOT modified — `git diff --exit-code services/nova-core/static/index.html` returns 0.
    - The two new frontend tests pass GREEN; all backend tests from Plan 01 still pass GREEN.
  </acceptance_criteria>
  <done>
    `admin.html` served with the two-card layout (5-service grid + per-user channel grid + Back-to-Dashboard anchor); `style.css` has an admin-specific append block; both new structure tests pass; `index.html` diff is empty. Committed with `feat(40-02): add admin.html + style.css admin block + structure tests`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create admin.js — SSE consumer + diffing renderer + escapeHtml + error banner</name>
  <files>services/nova-core/static/admin.js</files>
  <read_first>
    - services/nova-core/static/app.js (full — clone EventSource pattern at lines 13-32, escapeHtml pattern at lines 197-201, clock/update patterns, chat-user-tab toggle pattern, fadeIn animation reference)
    - services/nova-core/static/admin.html (Task 1 of this plan — IDs and class names it must target)
    - services/nova-core/tests/test_admin.py (Plan 01 tests — the payload contract admin.js must consume)
    - .planning/phases/40-admin-panel-page/40-UI-SPEC.md §SSE/Refresh Interaction, §Copywriting, §Accessibility
    - .planning/phases/40-admin-panel-page/40-RESEARCH.md §Pitfall 5, §Pitfall 6
  </read_first>
  <behavior>
    - On script load, admin.js opens `new EventSource('/admin/stream')` and registers `addEventListener('status', handler)` (named events — UI-SPEC §SSE/Refresh; distinct from dashboard's onmessage pattern)
    - Handler parses `event.data` as JSON; calls render functions for services and channels
    - Service cell renderer updates DOM only when the new status/detail differs from the current `data-status` attribute — unchanged cells keep their DOM (UI-SPEC §SSE/Refresh — no flash/re-animation); changed cells set `data-status`, update textContent of status word + detail, and add `fadeIn` animation class (reuse existing @keyframes fadeIn from style.css)
    - Channel cell renderer sets `linked`/`unlinked` class and `data-user`/`data-channel` attributes, updates the `.status-value` text to `Linked · {identifier}` or `Not linked`
    - Tab selector: clicking `Ruben` or `Meral` button toggles the `active` class on tabs and the `hidden` attribute on the channel cells for the inactive user (UI-SPEC §Page Structure)
    - escapeHtml helper: `const div = document.createElement('div'); div.textContent = text; return div.innerHTML;` — reused for any dynamic text set via innerHTML; prefer textContent for status/detail/identifier fields
    - onerror handler: show `#admin-error-banner` (remove `hidden` attribute); do NOT call `eventSource.close()`; do NOT call alert() or throw a modal (UI-SPEC §SSE/Refresh — browser auto-reconnects via EventSource)
    - on first successful status event: hide `#admin-error-banner` (set `hidden` attribute)
    - Live clock: reuse the existing clock pattern from app.js (if any) to update the `.time-area` — for admin.html the time-area shows only the clock, NO settings cog
    - Loading placeholders: each service cell starts with `Checking {Service Name}…` and a `.pulse`-animated placeholder (UI-SPEC §SSE/Refresh — reuse existing .chat-loading pulse keyframe); replaced with real status on first event
  </behavior>
  <action>
Create `services/nova-core/static/admin.js` as a standalone script (do NOT import app.js; do NOT share state with app.js — D-06). Use vanilla JS matching the existing `app.js` style (no modules, no framework — match `const eventSource = new EventSource(...)` at app.js:13).

Structure (top to bottom):

1. **escapeHtml helper** — clone from app.js:197-201. Used only when a string must be inserted via innerHTML (prefer textContent for static-status fields). Most admin rendering uses textContent so escapeHtml is mostly belt-and-suspenders.

2. **Tab selector wiring** — `document.querySelectorAll('.chat-user-tab').forEach(btn => btn.addEventListener('click', () => { ... }))`. On click: remove `active` from all tabs, add `active` to the clicked one; set `[hidden]` on every `.admin-channel-cell` whose `data-user` doesn't match the clicked user's `data-user`; remove `[hidden]` from cells that match. Default active tab is Ruben (matches admin.html's initial `active` class).

3. **Render functions:**
   - `renderServices(services)` — for each service key (ollama, postgres, caldav, ha, email): find the cell by `id="service-${key}"`; compare its current `data-status` attribute with `newStatus.status`; if equal, do nothing (preserve DOM — UI-SPEC §SSE/Refresh no-flash rule); if different, set `data-status` to new status, set the status-word textContent to the appropriate copy (per UI-SPEC §Copywriting — `Ready`/`Connected`/`Reachable` for ok, `Not ready`/`Disconnected`/`Unreachable` for down), set the detail textContent to `newStatus.detail`, and add the `fadeIn` animation class to trigger re-animation on change only. Use a `key` → `{okCopy, downCopy}` map to avoid a 5-branch switch.
   - `renderChannels(channels)` — for each user (`Ruben`, `Meral`) and each channel (`whatsapp`, `telegram`): find the cell by `id="channel-${user.toLowerCase()}-${channel}"` (or by `[data-user="${user}"][data-channel="${channel}"]`); set class list to include `linked` (if `channels[user][channel].linked`) or `unlinked` (otherwise); set `.status-value` textContent to `Linked · {identifier}` (if linked and identifier present) or `Linked` (if linked and no identifier — Telegram case) or `Not linked` (per UI-SPEC §Copywriting); only re-render if the cell's current `data-linked` attribute differs from the new value (reuse the diff pattern from services).
   - `renderState(state)` — orchestrator: `{services, channels} = state; renderServices(services); renderChannels(channels); hide('#admin-error-banner'); clearLoadingPlaceholders();`

4. **SSE listener** —
   ```
   const eventSource = new EventSource('/admin/stream');
   eventSource.addEventListener('status', (event) => {
     try {
       const data = JSON.parse(event.data);
       renderState(data);
     } catch (e) {
       console.error('Failed to parse admin SSE event:', e);
     }
   });
   eventSource.onerror = () => {
     document.getElementById('admin-error-banner').removeAttribute('hidden');
     // do NOT call eventSource.close() — browser auto-reconnects
     // do NOT call alert() or show a modal — UI-SPEC §SSE/Refresh
   };
   ```
   Match the existing app.js:13-32 style but use `addEventListener('status', ...)` instead of `onmessage = ...` because the admin endpoint emits NAMED events (per RESEARCH §State of the Art — Named SSE events).

5. **Live clock** — clone from app.js (or implement if not in app.js): setInterval(() => updateTime(), 1000); updateTime() sets `#clock` textContent to current time in HH:MM:SS format. Reuse the existing dashboard clock pattern if it exists in app.js (read app.js to find the exact pattern; if found, copy it verbatim with the same selector the admin.html `.time-area` defines).

6. **Loading placeholders** — `admin.html` already ships the cells with placeholder text (`Checking {Service Name}…`). admin.js's first successful render replaces these via `renderServices` and `renderChannels`. No additional initialization JS is needed — the placeholder is in the static HTML.

Anti-patterns to NOT do:
- Do NOT `addEventListener('message', ...)` — the admin endpoint emits named `event: status` events; use `addEventListener('status', ...)`.
- Do NOT call `alert()`, `confirm()`, or toggle a modal in `onerror`.
- Do NOT call `eventSource.close()` in `onerror` — browser auto-reconnects.
- Do NOT replace `innerHTML` of unchanged cells every 45s (UI-SPEC §SSE/Refresh Pitfall 6 — diff via `data-status` attribute).
- Do NOT add admin.js logic to `app.js` (D-06).
- Do NOT import or reference `/static/app.js` from admin.js or admin.html.
- Do NOT use `innerText` (Firefox has minor differences); use `textContent`.
- Do NOT include a fifth font weight or use inline `style="color: ..."` for status colors — use the `data-status` attribute + CSS class system.
- Do NOT modify `index.html` (D-07 gate).
- Do NOT use `f-string`-style template injections for untrusted text — every dynamic string goes through textContent (which auto-escapes) or escapeHtml.

Run `cd services/nova-core && python -m pytest tests/test_admin.py -x` — all tests still pass (admin.js has no pytest coverage beyond admin.html structure tests, which Task 1 already satisfied). Run the full suite `python -m pytest tests/ -x` — must remain green.

Commit with `feat(40-02): add admin.js SSE consumer + DOM-diff renderer + escapeHtml + error banner`.
  </action>
  <verify>
    <automated>cd services/nova-core && python -m pytest tests/test_admin.py -x && python -m pytest tests/test_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `admin.js` exists at `/static/admin.js` and is served with status 200.
    - `admin.js` contains `new EventSource('/admin/stream')` and `addEventListener('status', ` (named event — distinct from dashboard `onmessage`).
    - `admin.js` contains an `escapeHtml` function (mirrors app.js:197-201).
    - `admin.js` `onerror` handler does NOT call `eventSource.close()`, `alert()`, or `confirm()`. It toggles `#admin-error-banner`'s `hidden` attribute.
    - `admin.js` renderers compare `data-status` before mutating DOM; unchanged cells keep their existing DOM (only changed cells animate).
    - `admin.js` does NOT import or reference `/static/app.js`.
    - `admin.js` does NOT modify `index.html` or set `document.location`.
    - `index.html` is NOT modified in this plan's diff.
    - All Plan 01 + Plan 02 Task 1 tests pass GREEN.
    - `tests/test_dashboard.py` continues to pass (no regression — app.js and dashboard endpoints untouched).
  </acceptance_criteria>
  <done>
    `admin.js` opens an EventSource on the named `status` event, renders services and channels into the admin.html cells using DOM-diffing, shows a banner (not a modal) on SSE errors, auto-hides the banner on reconnect, and never touches app.js or index.html. Committed with `feat(40-02): add admin.js SSE consumer + DOM-diff renderer + escapeHtml + error banner`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ← `/admin/stream` SSE | Server pushes JSON `{services, channels}` into `admin.js`. The `detail` strings and `host` strings rendered into the DOM originate from server-side `_check_*` helpers (already filtered by Plan 01's `_host_only` and UI-SPEC copy contract). |
| Browser DOM ← admin.js rendering | Every dynamic string from the SSE payload is written to the DOM by `admin.js`. If a backend exception leaks a crafted string into `detail`, admin.js must NOT use it unescaped with `innerHTML`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-09 | Tampering / XSS | admin.js rendering into DOM via innerHTML | high | mitigate | Dynamic strings are set via `textContent` (auto-escapes HTML) by default. Where `innerHTML` is unavoidable, the `escapeHtml` helper (clone of app.js:197-201) is applied. Acceptance criterion prohibits raw `innerHTML =` with untrusted strings. Plan 01's `_host_only` already strips credentials from `host` fields, and the SSE `detail` field is constructed from UI-SPEC copy + host:port — but defense-in-depth requires the client to also escape. |
| T-40-10 | Denial of Service | admin.js re-animating every cell every 45s | low | mitigate | DOM-diffing renderer compares `data-status` before mutating; unchanged cells keep their DOM (no flash, no re-animation). UI-SPEC §SSE/Refresh locks this. Pitfall 6 in RESEARCH documents the failure mode. |
| T-40-11 | Information Disclosure | `/admin` link on dashboard | medium | mitigate | D-07 + D-09 lock: `index.html` is NOT modified. Plan-end gate: `git diff --exit-code services/nova-core/static/index.html`. The smoke test `test_admin_html_no_admin_link_on_dashboard` asserts `b"/admin" not in resp.content` when fetching `/static/index.html`. |
| T-40-12 | Spoofing / MITM | SSE event injected by attacker on the network | low | accept | D-08 accepts LAN trust. The SSE payload is read-only status (no write actions); a spoofed payload can at worst show wrong colors in the DOM — no data corruption, no auth bypass, no exec path. Caddy's `nova.local` route does not resolve off-LAN. |
| T-40-13 | Information Disclosure | Modal dialog revealing raw exception text on SSE failure | low | mitigate | UI-SPEC §SSE/Refresh locks: `onerror` shows the page-level `.chat-error` banner with the STATIC copy `Admin stream disconnected. Retrying…` — never the raw `event` or exception object. Acceptance criterion prohibits `alert()`/`confirm()`/modal toggling in `onerror`. |
| T-40-14 | Tampering | admin.js injected into other pages | low | accept | admin.js is loaded only by admin.html (which has a unique script tag). The existing app.js continues to load only on index.html. No shared state, no global pollution. StaticFiles mount serves the file as-is (no SSR → no template injection). |
| T-40-SC | Tampering | npm/pip/cargo installs | n/a | mitigate | Phase 40 introduces zero new packages. No install commands run. Slopcheck is a no-op for this phase. |
</threat_model>

<verification>
- `cd services/nova-core && python -m pytest tests/test_admin.py -x` — all backend (Plan 01) + frontend (Plan 02) tests GREEN
- `cd services/nova-core && python -m pytest tests/test_dashboard.py -x` — no regression to dashboard suite
- `cd services/nova-core && python -m pytest tests/ -x` — full suite green before `/gsd-verify-work`
- `git diff --exit-code services/nova-core/static/index.html` — index.html MUST be unmodified (D-07 gate; also proven by `test_admin_html_no_admin_link_on_dashboard`)
- Manual: open `/admin` in a browser, confirm the status board renders with green/red dots, the channel-status tab toggles between Ruben and Méral, the Back-to-Dashboard link navigates to `/`, and `/dashboard` does NOT contain a link to `/admin`
</verification>

<success_criteria>
- `/static/admin.html` is served with 200 and contains the two-card structure
- `/static/admin.js` is served with 200 and contains the EventSource + named-event pattern + escapeHtml
- `style.css` diff is append-only (no existing rules modified; only an admin block added)
- `index.html` diff is empty (D-07 gate)
- All test_admin.py tests pass GREEN
- No regression in test_dashboard.py or the full suite
- All admin.js dynamic strings go through textContent (auto-escape) or escapeHtml
- admin.js uses `addEventListener('status', ...)`, NOT `onmessage`
- admin.js `onerror` shows a banner, not a modal; does not call `eventSource.close()`
- The `/admin` redirect from Plan 01 now resolves to a real existing `admin.html` (Plan 01 created the route before the file; Plan 02 closes the loop)
</success_criteria>

<output>
Create `.planning/phases/40-admin-panel-page/40-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

Symbols added to `services/nova-core/static/admin.html`:
- Page skeleton cloned from `index.html` (head, dashboard-container, dashboard-header with `Nova <span>Admin</span>` title and `Live Connection` status indicator)
- `#back-to-dashboard` anchor (`<a href="/">`)
- `#admin-error-banner` (page-level error banner, initially hidden)
- `#system-status-panel` card with `.admin-service-grid` containing 5 cells: `#service-ollama`, `#service-postgres`, `#service-caldav`, `#service-ha`, `#service-email`
- `#channel-status-panel` card with `.chat-user-tabs` (Ruben / Méral — NO Household tab) and `.admin-channel-grid` containing 4 cells: `#channel-ruben-whatsapp`, `#channel-ruben-telegram`, `#channel-meral-whatsapp`, `#channel-meral-telegram`
- `<script src="/static/admin.js">` reference (NOT app.js)

Symbols added to `services/nova-core/static/admin.js`:
- `eventSource` (EventSource('/admin/stream'))
- `escapeHtml(text)` helper (clone of app.js:197-201)
- `renderServices(services)` (DOM-diffing renderer with data-status attribute compare)
- `renderChannels(channels)` (linked/unlinked renderer)
- `renderState(state)` orchestrator
- `onerror` handler (banner toggle, no close, no modal)
- Tab selector click wiring for `.chat-user-tab`

CSS additions to `services/nova-core/static/style.css` (append-only block):
- `.admin-service-grid` (3-col responsive grid)
- `.admin-service-cell` (flat inner panel — not a full glass-panel)
- `.admin-service-name`, `.admin-service-status`, `.admin-service-detail`
- `.admin-service-status.ok`, `.admin-service-status.down`
- `.admin-service-cell[data-status="ok"] .pulse-dot` (green + pulse animation)
- `.admin-service-cell[data-status="down"] .pulse-dot` (red + no animation — pulse reserved for healthy only)
- `.admin-channel-grid`, `.admin-channel-cell`, `.admin-channel-cell.unlinked`
- `.admin-channel-status .status-value` color rules

Tests appended to `services/nova-core/tests/test_admin.py`:
- `test_admin_html_served`
- `test_admin_html_structure`
- `test_admin_html_no_admin_link_on_dashboard` (D-07 smoke)

## Dependencies on Plan 01

This plan consumes the SSE payload shape locked by Plan 01's tests:
- `event: status\ndata: {services: {ollama, postgres, caldav, ha, email}}, channels: {Ruben: {whatsapp, telegram}, Meral: {whatsapp, telegram}}}`
- Each `services.{key}` is `{status: "ok"|"down", detail: str, host: str}`
- Each `channels.{user}.{channel}` is `{linked: bool, identifier: str}`

Plan 01's `tests/test_admin.py` enforces this contract; Plan 02's `admin.js` renders it. The shape is the integration boundary.