---
phase: 22-push-gateway-refactor
plan: 01
type: execute
wave: 1
depends_on:
  - 21-01
files_modified:
  - services/nova-core/app/channels/dispatcher.py
  - services/nova-core/app/channels/whatsapp.py
  - services/nova-core/app/channels/telegram.py
  - services/nova-core/app/scheduler.py
autonomous: true
requirements:
  - PUSH-01
  - PUSH-02
user_setup: []
must_haves:
  truths:
    - All proactive sends route through dispatcher.send_to_user()
    - Dispatcher selects channel from last_active_channel (not hardcoded)
    - WhatsApp fallback works when last_active=telegram but no Telegram identity
    - DND queued messages replay to the correct channel
  artifacts:
    - services/nova-core/app/channels/dispatcher.py: send_to_user routes by last_active_channel
    - services/nova-core/app/channels/whatsapp.py: ChannelAdapter implements send_message
    - services/nova-core/app/channels/telegram.py: ChannelAdapter implements send_message
  key_links:
    - All 5 scheduler call sites must use dispatcher, not direct adapter calls
    - process_queued_notifications reads channel column and routes to correct adapter
---

<objective>
Ensure all outbound proactive pushes route through the dispatcher to the user's last-active channel, with WhatsApp fallback and correct-channel DND queuing.

Purpose: The codebase already routes most sends through `dispatcher.send_to_user()`, but some legacy direct-adapter-call sites may remain. This plan audits all 5 scheduler call sites, confirms the dispatcher routing logic, and validates the WhatsApp fallback path.

Output:
- All scheduler call sites verified to use `send_to_user()`
- Dispatcher's last_active_channel routing confirmed correct
- WhatsApp fallback path (last_active=telegram but no Telegram identity) tested
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@services/nova-core/app/channels/dispatcher.py
@services/nova-core/app/channels/whatsapp.py
@services/nova-core/app/channels/telegram.py
@services/nova-core/app/scheduler.py
@services/nova-core/app/identity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 5 scheduler call sites and consolidate dispatcher routing</name>
  <files>
    services/nova-core/app/scheduler.py
    services/nova-core/app/channels/dispatcher.py
  </files>
  <action>
    **Audit scheduler call sites in `scheduler.py`:**

    The 5 proactive push call sites are:
    1. `send_morning_briefing_for_user()` (line 84) → `await send_to_user(user_name, briefing, proactive=True)` ✓
    2. `send_weekly_briefing_for_user()` (line 155) → `await send_to_user(user_name, briefing, proactive=True)` ✓
    3. `check_overdue_tasks()` (line 225) → `await send_to_user(assignee_name, alert, proactive=True)` ✓
    4. `check_new_emails()` (lines 258-268) → `await send_to_user(row["name"], alert, proactive=True)` — but note: this uses a hardcoded user list from `user_preferences JOIN users`, not from `dispatcher.send_to_user` itself. That's OK — `send_to_user` handles the channel routing.
    5. `process_queued_notifications()` (lines 287-300) → already routes by channel column:
       - `channel == "telegram"` → telegram adapter directly
       - `channel == "whatsapp"` (or default) → WhatsApp adapter

    All 5 call sites already use `send_to_user()` for initial dispatch. No changes needed to scheduler.py's call sites.

    **Audit dispatcher routing in `dispatcher.py`:**

    The existing `send_to_user()` (lines 11-49):
    - Reads `last_active_channel` from `user_preferences` ✓
    - Defaults to `'whatsapp'` when NULL ✓
    - Routes to Telegram adapter when `last_channel == "telegram"` and identity exists ✓
    - Falls back to WhatsApp when Telegram identity missing ✓

    **But** the dispatcher does NOT handle the case where `last_active_channel` is `'whatsapp'` — it falls through to the WhatsApp adapter correctly via:
    ```python
    from .whatsapp import adapter as whatsapp_adapter
    await whatsapp_adapter.send_message(user_name, text, proactive)
    ```
    This is correct — no change needed.

    **Key fix needed in dispatcher.py:**
    The DND check was added in Phase 16 (Task 1). Verify it's present and correctly using `last_channel` when queuing:
    ```python
    # After resolving last_channel, before routing:
    from ..identity import is_user_in_dnd
    if proactive:
        in_dnd = await is_user_in_dnd(user_name)
        if in_dnd:
            ... queue with channel = last_channel ...
    ```
    If the Phase 16 code was applied, this is already correct. If not (e.g. Phase 16 plan hasn't been executed yet), add it now. The `depends_on: [15-01]` in this plan's frontmatter is for plan-ordering; actual execution order is managed by the orchestrator.

    **For `process_queued_notifications()` in scheduler.py** — the Telegram path (lines 294-296) uses `telegram_adapter.send_message(name, msg_text, proactive=False)` which goes through `TelegramAdapter.send_message()` → `_send_to_chat_id()` with `proactive=False`. Since `proactive=False`, DND is skipped — which is correct because the DND window has already ended (that's why the notification was dequeued). No change needed.

    **Summary of changes for this task:** No production code changes needed — all call sites and routing are already correct. Add a verification test instead.
  </action>
  <verify>
    <automated>
      # Verify all 5 call sites use send_to_user
      rg "send_to_user" services/nova-core/app/scheduler.py | wc -l
      # Verify dispatcher has last_active_channel routing
      rg "last_active_channel" services/nova-core/app/channels/dispatcher.py
      # Verify WhatsApp fallback logic
      rg "falling back to WhatsApp" services/nova-core/app/channels/dispatcher.py
      # Verify process_queued_notifications routes by channel
      rg "channel == .telegram." services/nova-core/app/scheduler.py
      # Run tests
      pytest services/nova-core/tests/ -x -q 2>&1 | tail -10
    </automated>
  </verify>
  <done>
    - All 5 scheduler call sites confirmed to use `send_to_user()`
    - Dispatcher routes by `last_active_channel` with WhatsApp fallback
    - `process_queued_notifications` routes by channel column (telegram/whatsapp)
    - No hardcoded WhatsApp references in scheduler call sites
  </done>
</task>

<task type="auto">
  <name>Task 2: Ensure both channel adapters expose a uniform `send_message(user_name, text, proactive)` interface</name>
  <files>
    services/nova-core/app/channels/whatsapp.py
    services/nova-core/app/channels/telegram.py
  </files>
  <action>
    The dispatcher calls `adapter.send_message(user_name, text, proactive)` on both adapters. Verify the signatures match:

    **Telegram adapter** — `TelegramAdapter.send_message(self, user_name: str, text: str, proactive: bool = False)` (line 162) ✓
    - Signature: `(self, user_name, text, proactive=False)` ✓
    - Internally resolves `user_name` to `chat_id` via `channel_identities` ✓

    **WhatsApp adapter** — Check `WhatsAppAdapter.send_message()` signature:
    - Open `whatsapp.py` and locate the `send_message` method on `WhatsAppAdapter`.
    - Verify signature: `(self, user_name: str, text: str, proactive: bool = False)`.
    - Verify it resolves `user_name` to a phone number via `user_preferences.whatsapp_number`.
    - If the signature or resolution logic differs, fix to match:
      ```python
      async def send_message(self, user_name: str, text: str, proactive: bool = False) -> None:
          pool = await get_pool()
          async with pool.acquire() as conn:
              number = await conn.fetchval(
                  "SELECT whatsapp_number FROM user_preferences up JOIN users u ON up.user_id = u.id WHERE u.name = $1",
                  user_name
              )
              if not number:
                  print(f"[ERROR] No WhatsApp number for {user_name}, cannot send")
                  return
          await _send_to_number(number, text, user_name=user_name, proactive=proactive)
      ```
    - Import `get_pool` at the module level if not already present.

    Do NOT change the existing `_send_to_number()` function — it handles 24h compliance, DND queuing (now deprecated from WhatsApp side since Phase 16 moved it to dispatcher, but kept for defense-in-depth), and mock mode. Only ensure `send_message` calls it with the correct signature.

    If the adapter already has the correct signature and resolution logic, no changes are needed — just confirm in the verify step.
  </action>
  <verify>
    <automated>
      # Verify WhatsApp adapter send_message signature
      rg "async def send_message" services/nova-core/app/channels/whatsapp.py
      # Verify Telegram adapter send_message signature
      rg "async def send_message" services/nova-core/app/channels/telegram.py
      # Verify both resolve user_name to channel-specific identifier
      rg "SELECT.*whatsapp_number" services/nova-core/app/channels/whatsapp.py
      rg "SELECT.*channel_id" services/nova-core/app/channels/telegram.py
      # Run tests
      pytest services/nova-core/tests/ -x -q 2>&1 | tail -10
    </automated>
  </verify>
  <done>
    - Both `WhatsAppAdapter.send_message()` and `TelegramAdapter.send_message()` accept `(self, user_name, text, proactive)` with identical signatures
    - WhatsApp adapter resolves `user_name` to `whatsapp_number` from `user_preferences`
    - Telegram adapter resolves `user_name` to `chat_id` from `channel_identities`
    - All existing tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dispatcher → Channel adapter | Internal call path within trusted process |
| Channel adapter → External API | Outbound HTTPS to Meta/Telegram Bot API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Spoofing | send_to_user resolves last_active_channel from DB | low | accept | last_active_channel is updated by inbound handlers; only the user's own actions set it |
| T-22-02 | Elevation of Privilege | WhatsApp fallback on missing Telegram identity | low | accept | Fallback to WhatsApp ensures delivery; no privilege elevation |
| T-22-03 | Information Disclosure | Queued notification channel stored in DB | low | accept | Same `queued_notifications` table, same security model as existing DND queue |
| T-22-SC | Tampering | pip installs | medium | mitigate | No new packages — all dependencies already in project |
</threat_model>

<verification>
1. `rg "send_to_user" services/nova-core/app/scheduler.py` — 5+ matches (all call sites)
2. `rg "last_active_channel" services/nova-core/app/channels/dispatcher.py` — 1+ match (routing logic)
3. `pytest services/nova-core/tests/ -x -q` — all existing tests pass
4. Both channel adapters have matching `send_message(user_name, text, proactive)` signatures
</verification>

<success_criteria>
1. All 5 proactive push call sites route through `dispatcher.send_to_user()`
2. Dispatcher reads `last_active_channel` from DB and routes to the correct adapter
3. WhatsApp fallback fires when `last_active_channel='telegram'` but no Telegram identity exists
4. Both channel adapters implement `send_message(user_name, text, proactive)` uniformly
5. All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/22-push-gateway-refactor/22-01-SUMMARY.md` when all tasks complete
</output>
