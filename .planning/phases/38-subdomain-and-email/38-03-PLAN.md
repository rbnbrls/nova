---
phase: 38-subdomain-and-email
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - services/nova-core/alembic/versions/0011_drop_processed_emails.py
  - services/nova-core/tests/test_email.py
  - services/nova-core/tests/test_scheduler.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Migration 0011 drops processed_emails table; downgrade recreates it"
    - "test_email.py tests the IMAP fetch shape and SMTP send behavior"
    - "test_scheduler.py email tests mock IMAP instead of Graph and verify flag-based dedup"
  artifacts:
    - path: "services/nova-core/alembic/versions/0011_drop_processed_emails.py"
      provides: "Alembic migration dropping the processed_emails table"
    - path: "services/nova-core/tests/test_email.py"
      provides: "Tests for IMAP reader, SMTP sender, and preserved classification logic"
    - path: "services/nova-core/tests/test_scheduler.py"
      provides: "Updated email polling tests using IMAP mocks"
  key_links:
    - from: "services/nova-core/tests/test_email.py"
      to: "services/nova-core/app/tools/email.py"
      via: "imports fetch_emails_imap, send_email_message, classify_importance"
      pattern: "from app.tools.email import"
    - from: "services/nova-core/tests/test_scheduler.py"
      to: "services/nova-core/app/scheduler.py"
      via: "mocks fetch_emails_imap instead of fetch_emails_from_graph"
      pattern: "patch.*fetch_emails_imap"
---

<objective>
Drop the now-obsolete processed_emails table and rewrite the test suite for the IMAP/SMTP email backend.

Purpose: Clean up the DB schema and ensure the new email implementation is properly tested.
Output: Alembic migration 0011, rewritten test_email.py, updated test_scheduler.py.
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<!-- planner-discipline-allow: processed_emails -->

<context>
@.planning/phases/38-subdomain-and-email/38-CONTEXT.md
@.planning/phases/38-subdomain-and-email/38-RESEARCH.md
@services/nova-core/alembic/versions/0003_consolidate_inline_ddl.py
@services/nova-core/alembic/versions/0007_drop_whatsapp_verification_codes.py
@services/nova-core/tests/test_email.py
@services/nova-core/tests/test_scheduler.py
@services/nova-core/tests/conftest.py
@services/nova-core/app/tools/email.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Alembic migration 0011 to drop processed_emails table</name>
  <files>services/nova-core/alembic/versions/0011_drop_processed_emails.py</files>
  <read_first>
    services/nova-core/alembic/versions/0003_consolidate_inline_ddl.py
    services/nova-core/alembic/versions/0007_drop_whatsapp_verification_codes.py
  </read_first>
  <action>
    Create a new Alembic migration file at `services/nova-core/alembic/versions/0011_drop_processed_emails.py` per D-09.

    Follow the exact structure of migration 0007 (drop_whatsapp_verification_codes.py), which is the established pattern for dropping tables:

    - revision: "0011"
    - down_revision: "0010" (the latest existing migration, 0010_add_chore_recurrence)
    - No dependencies on other migrations

    The `upgrade()` function:
    - Calls `op.drop_table("processed_emails")` (simple drop, no data preservation needed per D-08/D-09)

    The `downgrade()` function:
    - Recreates the `processed_emails` table with the EXACT same schema as defined in migration 0003's upgrade():
      - email_id: sa.String(255), nullable=False, primary key
      - processed_at: sa.DateTime(timezone=True), server_default=sa.text("now()"), nullable=False

    Include a descriptive docstring at module top: "Drop processed_emails table (replaced by IMAP flags — Phase 38)"

    Include all necessary imports: from typing import Sequence, Union; from alembic import op; import sqlalchemy as sa.

    This follows the exact same pattern as 0007 which drops a table in upgrade() and recreates it in downgrade().
  </action>
  <verify>
    <automated>cd services/nova-core && python -c "
import ast, sys
with open('alembic/versions/0011_drop_processed_emails.py') as f:
    tree = ast.parse(f.read())

# Check revision
for node in ast.walk(tree):
    if isinstance(node, ast.Assign):
        for target in node.targets:
            if isinstance(target, ast.Name) and target.id == 'revision':
                rev = node.value.value if isinstance(node.value, ast.Constant) else None
                assert rev == '0011', f'Expected revision 0011, got {rev}'
            if isinstance(target, ast.Name) and target.id == 'down_revision':
                dr = node.value.value if isinstance(node.value, ast.Constant) else None
                assert dr == '0010', f'Expected down_revision 0010, got {dr}'

# Check functions
content = open('alembic/versions/0011_drop_processed_emails.py').read()
assert 'def upgrade()' in content, 'Missing upgrade()'
assert 'def downgrade()' in content, 'Missing downgrade()'
assert 'drop_table' in content, 'Missing drop_table call'
assert 'create_table' in content, 'Missing create_table in downgrade'
assert 'processed_emails' in content, 'Missing processed_emails reference'
print('Migration 0011 structure valid')
"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at `alembic/versions/0011_drop_processed_emails.py`
    - revision="0011", down_revision="0010"
    - upgrade() calls `op.drop_table("processed_emails")`
    - downgrade() recreates `processed_emails` with columns: email_id (String(255), PK), processed_at (DateTime(tz), server_default=now())
    - Matches the structure and style of migration 0007
    - File imports sa, op, and typing modules
  </acceptance_criteria>
  <done>Migration created; `alembic upgrade head` drops processed_emails; `alembic downgrade -1` recreates it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite test_email.py — remove Graph tests, add IMAP/SMTP tests</name>
  <files>services/nova-core/tests/test_email.py</files>
  <read_first>
    services/nova-core/tests/test_email.py
    services/nova-core/tests/conftest.py
    services/nova-core/app/tools/email.py
    services/nova-core/app/config.py
  </read_first>
  <behavior>
    - Test 1 (D-01/D-02): nova_email property — Settings(nova_domain="test.com").nova_email == "nova@test.com" and Settings().nova_email == ""
    - Test 2 (D-15): fetch_emails_imap returns correct dict shape — when IMAP host empty (mock), returns list of dicts each with keys: id, subject, from, preview, unread
    - Test 3 (D-15): fetch_emails_imap mock data contains expected emails — verifies the 4 hardcoded mock emails are returned
    - Test 4 (D-14): send_email tool success — mocks aiosmtplib.send to return code 250; verifies tool returns confirmation string with recipient and subject
    - Test 5 (D-14): send_email tool failure — mocks aiosmtplib.send to return code 550; verifies tool returns failure string with error code
    - Test 6 (D-08): _mark_email_processed called with correct flags — mocks aioimaplib, verifies UID STORE command includes $NovaProcessed flag
    - Test 7 (D-10): classify_importance keyword Dutch — preserved from existing test, "Factuur juli 2026" returns True
    - Test 8 (D-10): classify_importance keyword English — preserved from existing test, "Invoice for July" returns True
    - Test 9 (D-10): classify_importance keyword preview — preserved from existing test, "school" in preview returns True
    - Test 10 (D-10): classify_importance falls back to LLM — preserved from existing test, mocks llm.chat
    - Test 11 (D-10): classify_importance conservative on LLM error — preserved from existing test, RuntimeError returns True
    - Test 12 (D-13): list_recent_emails uses IMAP — mocks fetch_emails_imap to return mock data; verifies [IMPORTANT] tag appears for keyword-matching emails
    - Test 13 (D-13): list_recent_emails empty — mocks fetch_emails_imap returning []; verifies "No recent emails." message
    - Test 14 (D-15): Fetch uses BODY.PEEK — mocks aioimaplib, verifies FETCH command contains "BODY.PEEK" (not "BODY[")
  </behavior>
  <action>
    Rewrite `services/nova-core/tests/test_email.py` completely. The file currently tests MS Graph behavior — replace with IMAP/SMTP tests.

    **REMOVE entirely:**
    - EMAIL-03 tests: test_graph_url_uses_mailbox_email() and test_graph_url_not_tenant_wide() — these test MS Graph URL construction which no longer exists
    - All references to `fetch_emails_from_graph`, `azure_mailbox_email`, graph.microsoft.com
    - All `@patch.object(settings, "azure_mailbox_email", ...)` patterns

    **KEEP and update imports:**
    - The existing imports for classify_importance, list_recent_emails
    - Add: `from app.tools.email import fetch_emails_imap, send_email_message, _mark_email_processed`
    - Add: `from app.config import Settings`
    - Add: `from unittest.mock import AsyncMock, MagicMock, patch, call` (update existing)
    - Remove: `fetch_emails_from_graph` from imports
    - The conftest.py `_mock_user_memories` fixture is auto-applied and handles the agent.get_user_memories mock automatically

    **KEEP exactly as-is (copy the function bodies without modification):**
    - test_classify_importance_keyword_dutch() — EMAIL-01
    - test_classify_importance_keyword_english() — EMAIL-01
    - test_classify_importance_keyword_preview() — EMAIL-01
    - test_classify_importance_falls_back_to_llm() — EMAIL-01
    - test_classify_importance_conservative_on_llm_error() — EMAIL-01

    **UPDATE existing tests:**
    - test_list_recent_emails_shows_importance_tag() — change the `patch("app.tools.email.llm.chat"...)` approach to also mock `fetch_emails_imap` (since list_recent_emails now calls fetch_emails_imap instead of fetch_emails_from_graph). Mock `fetch_emails_imap` to return a list containing the "School update" and "Factuur" emails. Keep the assertions that check for [IMPORTANT] tags.
    - test_list_recent_emails_empty() — change from mocking `fetch_emails_from_graph` to mocking `fetch_emails_imap` returning `[]`.

    **ADD new tests:**
    - `test_nova_email_property()` — create `Settings(nova_domain="7rb.nl")`, assert `nova_email == "nova@7rb.nl"`. Create `Settings()`, assert `nova_email == ""`.
    - `test_fetch_emails_imap_mock_shape()` — call `fetch_emails_imap(limit=10)` (no IMAP config → returns mock data). Verify result is a list. Verify each dict has keys: "id", "subject", "from", "preview", "unread". Verify len(result) > 0.
    - `test_fetch_emails_imap_mock_content()` — call `fetch_emails_imap(limit=10)`. Verify the mock data contains the 4 expected emails (msg_1 School update, msg_2 CalDAV, msg_3 Factuur, msg_4 Spam).
    - `test_send_email_tool_success()` — `@patch("app.tools.email.aiosmtplib.send", new_callable=AsyncMock)` to return a MagicMock with `code=250, message="OK"`. Call send_email_message("test@example.com", "Test Subject", "Test body"). Assert result["success"] is True and result["code"] == 250.
    - `test_send_email_tool_failure()` — `@patch("app.tools.email.aiosmtplib.send", new_callable=AsyncMock)` to return a MagicMock with `code=550, message="Rejected"`. Call send_email_message("test@example.com", "Test Subject", "Test body"). Assert result["success"] is False and result["code"] == 550.
    - `test_send_email_from_address()` — mock aiosmtplib.send, call send_email_message, verify the EmailMessage `From` header equals "nova@" + the configured domain. Use `@patch.object(settings, "nova_domain", "test.com")`.
    - `test_imap_uses_body_peek()` — mock aioimaplib.IMAP4_SSL, set up the mock to return appropriate responses. Call fetch_emails_imap with a mocked IMAP host. Verify the `uid("fetch", ...)` call arguments contain "BODY.PEEK" (not "BODY[" without PEEK). This test guarantees Pitfall 2 is avoided.
    - `test_imap_uses_uid_not_seq()` — mock aioimaplib.IMAP4_SSL and verify that search/fetch/store operations use `imap.uid()` method (not direct `imap.search()` / `imap.fetch()` / `imap.store()` which use sequence numbers). This test guarantees Pitfall 1 is avoided.
    - `test_mark_email_processed_flags()` — mock aioimaplib.IMAP4_SSL, call _mark_email_processed("42"). Verify `imap.uid("store", "42", "+FLAGS", ...)` is called with arguments containing "$NovaProcessed".

    **Test file header update:**
    Change the module docstring from "Tests for the email tool (MS Graph-backed)." to "Tests for the email tool (IMAP/SMTP-backed, Phase 38)."

    **Mock patterns to follow:**
    - For mocking aioimaplib: `@patch("app.tools.email.aioimaplib.IMAP4_SSL", autospec=True)` — use `mock_imap = imap_cls.return_value` then configure `mock_imap.wait_hello_from_server = AsyncMock()`, `mock_imap.login = AsyncMock()`, `mock_imap.select = AsyncMock()`, `mock_imap.uid = AsyncMock()`, `mock_imap.logout = AsyncMock()`
    - For the `imap.uid` mock, configure `side_effect` based on the command: for "search" return `("OK", [b"1 2 3"])`, for "fetch" return `("OK", [b"..."])`, for "store" return `("OK", [b"..."])`
    - For mocking aiosmtplib: `@patch("app.tools.email.aiosmtplib.send", new_callable=AsyncMock)`
    - For mock data tests (no IMAP host): use `@patch.object(settings, "nova_imap_host", "")` to trigger mock fallback path

    **Test isolation:**
    - The conftest.py `_mock_user_memories` fixture is auto-applied via autouse=True — no need to patch `agent.get_user_memories` in any test.
    - Import `from app.config import settings` to use `@patch.object(settings, ...)` for config mocking.
  </action>
  <verify>
    <automated>cd services/nova-core && python -m pytest tests/test_email.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All EMAIL-01 tests pass unchanged (5 keyword classification tests)
    - New tests: test_fetch_emails_imap_mock_shape, test_fetch_emails_imap_mock_content pass
    - New tests: test_send_email_tool_success, test_send_email_tool_failure, test_send_email_from_address pass
    - New tests: test_imap_uses_body_peek, test_imap_uses_uid_not_seq, test_mark_email_processed_flags pass
    - Updated tests: test_list_recent_emails_shows_importance_tag, test_list_recent_emails_empty pass
    - No references to fetch_emails_from_graph, azure_*, MS Graph in the test file
    - Full test suite for test_email.py: `python -m pytest tests/test_email.py -x` exits 0
  </acceptance_criteria>
  <done>test_email.py fully rewritten; all classification tests preserved, all new IMAP/SMTP tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update test_scheduler.py — adapt email polling tests for IMAP</name>
  <files>services/nova-core/tests/test_scheduler.py</files>
  <read_first>
    services/nova-core/tests/test_scheduler.py
    services/nova-core/app/scheduler.py
    services/nova-core/app/tools/email.py
    services/nova-core/tests/conftest.py
  </read_first>
  <behavior>
    - Test 1 (D-08): Email polling uses IMAP flag dedup — mock fetch_emails_imap returns important emails, verify _mark_email_processed is called with the email UID after notification
    - Test 2 (D-08/D-09): Email polling does NOT touch processed_emails table — verify no INSERT INTO processed_emails or SELECT from processed_emails occurs
    - Test 3: Second poll skips already-processed emails — mock fetch_emails_imap to return same email twice, verify notifications only sent once
    - Test 4 (existing): Briefing tests pass with fetch_emails_imap mock — update test_morning_briefing_includes_tasks and test_morning_briefing_empty_states to mock fetch_emails_imap instead of fetch_emails_from_graph
  </behavior>
  <action>
    Update `services/nova-core/tests/test_scheduler.py` to use IMAP mocks instead of Graph mocks.

    **Test: test_email_polling_deduplication() (lines 101-136) — REWRITE:**
    Replace the DB-based dedup test with IMAP flag-based dedup:
    - Mock `fetch_emails_imap` (from `app.scheduler.fetch_emails_imap`) to return an important email: `{"id": "42", "subject": "Factuur July 2026", "from": "billing@energy.nl", "preview": "Invoice details...", "unread": True}`
    - Mock `_mark_email_processed` (from `app.scheduler._mark_email_processed`) as AsyncMock
    - Mock `send_to_user` (from `app.scheduler.send_to_user`) as AsyncMock
    - Remove ALL mock code related to `processed_emails` table: no mock_conn.fetchval, no INSERT INTO processed_emails assertions
    - Replace: instead of checking `mock_conn.execute` was called with `"INSERT INTO processed_emails"`, verify `_mark_email_processed` was called with `"42"` (the email UID)
    - Verify `send_to_user` was called (notification sent for important email)
    - Second poll: mock `fetch_emails_imap` to return empty list (mail already has $NovaProcessed flag so UNKEYWORD NovaProcessed search returns nothing), verify `send_to_user` NOT called and `_mark_email_processed` NOT called
    - Import: add `_mark_email_processed` to the imports at top of file or reference via `app.scheduler._mark_email_processed` in patches

    **Test: test_email_polling_uses_imap (NEW, add after test_email_polling_deduplication):**
    - `@patch("app.scheduler.fetch_emails_imap", new_callable=AsyncMock)` returns `[]`
    - `@patch("app.scheduler._mark_email_processed", new_callable=AsyncMock)` as sentinel
    - Call `check_new_emails()`
    - Verify `fetch_emails_imap` was called (assert_called_once or assert_called)
    - Verify no DB pool interaction for dedup (no `get_pool` called specifically for processed_emails purposes — the get_pool call for fetching users is still valid)

    **Test: test_email_polling_no_processed_emails_table (NEW):**
    - Verify that `check_new_emails()` does not attempt to query or insert into `processed_emails`
    - Mock `fetch_emails_imap` to return `[]`
    - Verify the string "processed_emails" does not appear in the scheduler module's source for the check_new_emails function
    - This is a static assertion: `grep -c 'processed_emails' app/scheduler.py == 0` or equivalent

    **Tests: test_morning_briefing_includes_tasks() and test_morning_briefing_empty_states() — UPDATE:**
    - Change `@patch("app.scheduler.fetch_emails_from_graph", return_value=[])` to `@patch("app.scheduler.fetch_emails_imap", return_value=[])`
    - Change `@patch("app.scheduler.classify_importance", return_value=False)` — keep as-is (classify_importance is still used)
    - No other changes needed — these tests mock fetch to return empty and verify the briefing text content

    **Tests: test_proactive_send_uses_template() — UPDATE:**
    - Same change: `@patch("app.scheduler.fetch_emails_from_graph", return_value=[])` → `@patch("app.scheduler.fetch_emails_imap", return_value=[])`
    - Keep the rest of the test identical

    **Tests to KEEP unchanged:**
    - test_inbound_updates_last_inbound_at — no email dependency
    - test_outbound_whatsapp_compliance_checks — no email dependency
    - test_run_briefing_scheduler_triggers — no email dependency

    **IMPORTANT: comfitest.py fixture**
    - conftest.py's `_mock_user_memories` auto-fixture patches `app.agent.get_user_memories`, which is imported by `app.scheduler` (via `from .db import get_user_memories`). The scheduler test mocks `app.scheduler.send_to_user` so the agent mock doesn't interfere. Keep the test structure the same — just change which email fetch function is mocked.
  </action>
  <verify>
    <automated>cd services/nova-core && python -m pytest tests/test_scheduler.py -x -v -k "email or briefing or proactive" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test_email_polling_deduplication verifies _mark_email_processed called with correct UID, and second poll finds no emails (dedup works)
    - test_email_polling_uses_imap verifies fetch_emails_imap is called (not fetch_emails_from_graph)
    - No test references `fetch_emails_from_graph` or `processed_emails` (except in comments describing what was removed)
    - test_morning_briefing_includes_tasks, test_morning_briefing_empty_states, test_proactive_send_uses_template all pass
    - All non-email tests (whatsapp compliance, last_inbound_at, briefing scheduler triggers) continue to pass
    - Full scheduler test suite: `python -m pytest tests/test_scheduler.py -x` exits 0
  </acceptance_criteria>
  <done>test_scheduler.py updated; email polling tests use IMAP mocks with flag-based dedup verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → Postgres | Schema change: DROP TABLE processed_emails |
| Test mocks → IMAP protocol | Tests verify correct IMAP command patterns (UID, BODY.PEEK) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-09 | Tampering | Alembic migration 0011 | low | accept | Migration is a simple DROP TABLE; downgrade recreates the table from the exact schema in 0003; no data migration risk since IMAP flags are the new source of truth (D-08) |
| T-38-10 | Information Disclosure | tests/test_email.py | low | accept | Test file uses mock data only — no real credentials or email content; .env.test is not needed since tests use @patch.object(settings, ...) |
</threat_model>

<verification>
```bash
cd services/nova-core

# Verify migration structure
python -c "
import ast
with open('alembic/versions/0011_drop_processed_emails.py') as f:
    tree = ast.parse(f.read())
for node in ast.walk(tree):
    if isinstance(node, ast.Assign):
        for t in node.targets:
            if isinstance(t, ast.Name) and t.id == 'revision':
                assert node.value.value == '0011', 'Wrong revision'
            if isinstance(t, ast.Name) and t.id == 'down_revision':
                assert node.value.value == '0010', 'Wrong down_revision'
print('Migration OK')
"

# Run email tests
python -m pytest tests/test_email.py -x -v

# Run scheduler email-related tests
python -m pytest tests/test_scheduler.py -x -v -k "email or briefing or proactive"

# Verify no processed_emails refs in test files
! grep -rn 'processed_emails' tests/test_email.py tests/test_scheduler.py && echo "No DB dedup refs in tests"
! grep -rn 'fetch_emails_from_graph' tests/test_email.py tests/test_scheduler.py && echo "No Graph refs in tests"
```
</verification>

<success_criteria>
- [ ] Migration 0011 drops processed_emails (upgrade) and recreates it (downgrade)
- [ ] test_email.py: all classification tests preserved and passing
- [ ] test_email.py: new IMAP fetch shape, SMTP send, BODY.PEEK usage, UID operations tests passing
- [ ] test_scheduler.py: email polling tests use IMAP mocks with flag dedup verification
- [ ] test_scheduler.py: all non-email tests continue to pass
- [ ] Zero references to fetch_emails_from_graph, azure_*, or processed_emails in test files
</success_criteria>

<output>
Create `.planning/phases/38-subdomain-and-email/38-03-SUMMARY.md` when done
</output>
