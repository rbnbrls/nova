---
phase: 38-subdomain-and-email
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/nova-core/app/config.py
  - services/nova-core/requirements.txt
  - .env.example
autonomous: true
requirements: []

must_haves:
  truths:
    - "Settings object exposes nova_domain, nova_imap_*, nova_smtp_* fields with correct defaults"
    - "All azure_* fields are removed from Settings class"
    - "nova_email property derives from nova_domain"
    - "aioimaplib and aiosmtplib are declared as dependencies"
  artifacts:
    - path: "services/nova-core/app/config.py"
      provides: "Settings class with email config fields"
      contains:
        - "nova_domain: str"
        - "nova_imap_host: str"
        - "nova_imap_port: int = 993"
        - "nova_smtp_port: int = 587"
        - "def nova_email"
    - path: "services/nova-core/requirements.txt"
      provides: "Package declarations"
      contains:
        - "aioimaplib==2.0.1"
        - "aiosmtplib==5.1.2"
  key_links:
    - from: "services/nova-core/app/config.py"
      to: "services/nova-core/app/tools/email.py"
      via: "email.py imports settings.nova_domain, settings.nova_imap_*, settings.nova_smtp_*"
      pattern: "from ..config import settings"

user_setup:
  - service: "Email provider (IMAP/SMTP)"
    why: "Nova reads and sends email through the user's existing email provider"
    env_vars:
      - name: NOVA_DOMAIN
        source: "Your domain (e.g., 7rb.nl) — no default; must be set"
      - name: NOVA_IMAP_HOST
        source: "IMAP server hostname from your email provider"
      - name: NOVA_IMAP_PORT
        source: "IMAP port (default 993)"
      - name: NOVA_IMAP_USER
        source: "Email account username (typically nova@your-domain)"
      - name: NOVA_IMAP_PASS
        source: "Email account password or app-specific password"
      - name: NOVA_SMTP_HOST
        source: "SMTP server hostname from your email provider"
      - name: NOVA_SMTP_PORT
        source: "SMTP port (default 587)"
      - name: NOVA_SMTP_USER
        source: "SMTP username (same as IMAP user for most providers)"
      - name: NOVA_SMTP_PASS
        source: "SMTP password (same as IMAP pass for most providers)"
      - name: NOVA_SMTP_USE_TLS
        source: "Set to true for STARTTLS on port 587 (default)"
---

<objective>
Replace the MS Graph config with IMAP/SMTP configuration, add required async libraries.

Purpose: Foundation for the email refactor — all downstream work depends on these config fields and packages being available.
Output: Updated config.py with new env vars, updated requirements.txt with aioimaplib + aiosmtplib, updated .env.example.
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-subdomain-and-email/38-CONTEXT.md
@.planning/phases/38-subdomain-and-email/38-RESEARCH.md
@services/nova-core/app/config.py
@services/nova-core/requirements.txt
@.env.example
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update config.py with email config, remove azure_*</name>
  <files>services/nova-core/app/config.py</files>
  <read_first>services/nova-core/app/config.py</read_first>
  <action>
    Update the Settings class in config.py per D-01 through D-06:

    REMOVE these four fields (and their comments):
    - azure_tenant_id: str = ""
    - azure_client_id: str = ""
    - azure_client_secret: str = ""
    - azure_mailbox_email: str = ""
    Also remove the comment line "# MS Graph Email Configuration".

    ADD in the same location (after the HA section, before the household section):
    - nova_domain: str = "" — no default, must be explicitly set (per D-01)
    - nova_imap_host: str = "" — IMAP server hostname (per D-06)
    - nova_imap_port: int = 993 — IMAPS default (per D-06)
    - nova_imap_user: str = "" — IMAP login username (per D-06)
    - nova_imap_pass: str = "" — IMAP login password (per D-06)
    - nova_imap_use_ssl: bool = True — connect via SSL on port 993 (per D-06)
    - nova_smtp_host: str = "" — SMTP relay hostname (per D-03)
    - nova_smtp_port: int = 587 — STARTTLS default (per D-03)
    - nova_smtp_user: str = "" — SMTP auth username (per D-03)
    - nova_smtp_pass: str = "" — SMTP auth password (per D-03)
    - nova_smtp_use_tls: bool = True — STARTTLS negotiation (per D-03)

    ADD a @property after the database_url property:
    ```python
    @property
    def nova_email(self) -> str:
        """Derived email identity: nova@{NOVA_DOMAIN} (per D-02)."""
        if not self.nova_domain:
            return ""
        return f"nova@{self.nova_domain}"
    ```

    Follow the exact existing style: fields grouped with a section comment, type-hinted with default value, consistent ordering.
    All new fields should use the same pattern as existing fields (no Field() alias, use pydantic-settings default env var mapping).
  </action>
  <verify>
    <automated>python -c "from app.config import Settings; s = Settings(); assert hasattr(s, 'nova_domain'); assert hasattr(s, 'nova_email'); assert s.nova_imap_port == 993; assert s.nova_smtp_port == 587; assert not hasattr(s, 'azure_tenant_id'); assert not hasattr(s, 'azure_client_id'); assert not hasattr(s, 'azure_client_secret'); assert not hasattr(s, 'azure_mailbox_email'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Settings class has nova_domain, nova_imap_host, nova_imap_port (993), nova_imap_user, nova_imap_pass, nova_imap_use_ssl (True)
    - Settings class has nova_smtp_host, nova_smtp_port (587), nova_smtp_user, nova_smtp_pass, nova_smtp_use_tls (True)
    - Settings class has nova_email property returning "nova@{nova_domain}" or "" if domain unset
    - Settings class does NOT have azure_tenant_id, azure_client_id, azure_client_secret, azure_mailbox_email
    - `python -c "from app.config import Settings; s = Settings(nova_domain='test.com'); assert s.nova_email == 'nova@test.com'"` passes
  </acceptance_criteria>
  <done>Config updated; all azure_* fields removed; new email config fields present with correct defaults and nova_email property.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add aioimaplib and aiosmtplib to requirements.txt</name>
  <files>services/nova-core/requirements.txt</files>
  <read_first>services/nova-core/requirements.txt</read_first>
  <action>
    Add two new dependencies to requirements.txt, appending after the last existing entry (pip-audit):

    aioimaplib==2.0.1
    aiosmtplib==5.1.2

    These are the async IMAP and SMTP libraries chosen in RESEARCH.md. Both are pure Python, zero-runtime-deps packages with verified PyPI legitimacy.
    The Dockerfile `pip install -r requirements.txt` step will pick them up automatically — no Dockerfile changes needed.
  </action>
  <verify>
    <automated>grep -E '^aioimaplib==2\.0\.1$' services/nova-core/requirements.txt && grep -E '^aiosmtplib==5\.1\.2$' services/nova-core/requirements.txt && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains exact line: aioimaplib==2.0.1
    - requirements.txt contains exact line: aiosmtplib==5.1.2
    - No other lines changed or removed
  </acceptance_criteria>
  <done>Both packages declared; Dockerfile install step will resolve them on next build.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update .env.example with email config replacing MS Graph</name>
  <files>.env.example</files>
  <read_first>.env.example</read_first>
  <action>
    Replace the MS Graph section in .env.example (lines starting with "# --- Microsoft Graph" through "MSGRAPH_SHARED_MAILBOX=household@example.com") with a new Email section:

    Replace the four MSGRAPH_* lines with approximately:
    ```
    # --- Email (IMAP + SMTP, replaces MS Graph) — Phase 38 ---
    NOVA_DOMAIN=                      # e.g., 7rb.nl — no default; must be set
    NOVA_IMAP_HOST=                   # IMAP server hostname
    NOVA_IMAP_PORT=993                # IMAPS port
    NOVA_IMAP_USER=nova               # mailbox user
    NOVA_IMAP_PASS=change-me          # mailbox password
    NOVA_IMAP_USE_SSL=true            # connect via SSL (993)
    NOVA_SMTP_HOST=                   # SMTP relay hostname
    NOVA_SMTP_PORT=587                # STARTTLS port
    NOVA_SMTP_USER=nova               # SMTP auth user
    NOVA_SMTP_PASS=change-me          # SMTP auth password
    NOVA_SMTP_USE_TLS=true            # negotiate STARTTLS (587)
    ```

    The section comment should reference Phase 38 and note "replaces MS Graph".
    Keep the rest of the file unchanged.
  </action>
  <verify>
    <automated>grep -c 'NOVA_DOMAIN=' .env.example && grep -c 'NOVA_IMAP_HOST=' .env.example && grep -c 'NOVA_SMTP_HOST=' .env.example && ! grep -c 'MSGRAPH_' .env.example && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains NOVA_DOMAIN, NOVA_IMAP_HOST, NOVA_IMAP_PORT, NOVA_IMAP_USER, NOVA_IMAP_PASS, NOVA_IMAP_USE_SSL
    - .env.example contains NOVA_SMTP_HOST, NOVA_SMTP_PORT, NOVA_SMTP_USER, NOVA_SMTP_PASS, NOVA_SMTP_USE_TLS
    - .env.example does NOT contain any MSGRAPH_ prefixed variable
    - Section comment references Phase 38 and "replaces MS Graph"
  </acceptance_criteria>
  <done>.env.example reflects the new email configuration; MS Graph entries removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env file → config.py | Secrets cross from environment into application memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Information Disclosure | config.py / .env | medium | mitigate | `NOVA_IMAP_PASS` and `NOVA_SMTP_PASS` are env-only, never logged or committed; `.env` is in `.gitignore`; `.env.example` uses placeholder values only |
| T-38-SC | Tampering | pip install (aioimaplib, aiosmtplib) | high | mitigate | Both packages verified legitimate via RESEARCH.md Package Legitimacy Audit (9-10 year history, verified PyPI/ GitHub repos, no postinstall scripts); slopcheck gate passes |
</threat_model>

<verification>
```bash
cd services/nova-core && python -c "
from app.config import Settings
s = Settings(nova_domain='test.com')
assert s.nova_email == 'nova@test.com'
assert s.nova_imap_port == 993
assert s.nova_smtp_port == 587
assert s.nova_imap_use_ssl is True
assert s.nova_smtp_use_tls is True
assert not hasattr(s, 'azure_tenant_id')
print('Config OK')
"
grep -E '^aioimaplib==2\.0\.1$' requirements.txt && echo 'aioimaplib OK'
grep -E '^aiosmtplib==5\.1\.2$' requirements.txt && echo 'aiosmtplib OK'
grep 'NOVA_DOMAIN=' ../../.env.example && echo '.env.example OK'
! grep 'MSGRAPH_' ../../.env.example && echo 'No MS Graph leftovers'
```
</verification>

<success_criteria>
- [ ] config.py exposes all email config fields with correct defaults
- [ ] azure_* fields completely removed from config.py
- [ ] nova_email property works for both set and unset domain
- [ ] requirements.txt includes aioimaplib 2.0.1 and aiosmtplib 5.1.2
- [ ] .env.example updated to reflect new email config
</success_criteria>

<output>
Create `.planning/phases/38-subdomain-and-email/38-01-SUMMARY.md` when done
</output>
