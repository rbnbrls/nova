---
phase: 07-evaluation-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/nova-core/tests/test_evals.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
user_setup: []

must_haves:
  truths:
    - test_evals.py contains all 11 eval scenarios (7 existing + 4 new)
    - Each scenario runs deterministically under pytest with mocked LLM
    - Calendar creation scenario tests resolved datetime -> create_event tool call
    - Task-with-deadline scenario tests due_at parameter -> add_task tool call
    - Priority task scenario tests priority=high -> add_task tool call
    - Weather/proactive refusal scenario tests graceful refusal without tool calls
    - Suite is discoverable by pytest collection (test_eval_suite_is_discoverable_by_pytest)
  artifacts:
    - services/nova-core/tests/test_evals.py
  key_links:
    - Mock app.llm.chat return value dict must match run_agent expected message format ({role, content, tool_calls})
    - Mock app.tools.call_tool return value string must be compatible with run_agent tool-result feed
---

<objective>
Add 4 new golden-conversation eval scenarios to the existing eval suite, covering calendar creation, task-with-deadline, priority task, and weather refusal. Each scenario follows the established dual-path pattern (real model when `llm.is_ready()`, deterministic mock fallback in CI). The discoverability test is updated to reflect the full 11-scenario suite.

Purpose: Ensure the agent loop correctly dispatches create_event, add_task with due_at, add_task with priority, and graceful-out-of-scope refusal — gating changes to the system prompt, tool specs, or model.
Output: Updated `services/nova-core/tests/test_evals.py` with 11 scenarios, all passing under `pytest`.
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-evaluation-suite/07-CONTEXT.md
@services/nova-core/tests/test_evals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 new eval scenarios to test_evals.py</name>
  <files>services/nova-core/tests/test_evals.py</files>
  <action>
    Insert 4 new async test functions before the existing `test_eval_suite_is_discoverable_by_pytest` function (before line 216 in current file, after the last scenario section). Each follows the exact pattern of the 7 existing tests:

    1. **`test_eval_calendar_creation_scenario`** ("create an event tomorrow at 3pm"):
       - Docstring: "Test calendar creation via create_event tool."
       - `if not await llm.is_ready():` — mock LLM returns a single tool_call dict for `create_event` with arguments containing resolved datetime fields (`summary`, `start`, `end` as ISO strings). Patch `app.llm.chat` with AsyncMock returning that dict. Patch `app.tools.call_tool` with AsyncMock returning "Mocked event created". Assert the final response contains an event-related keyword or "created".
       - Real-model path: `resp = await run_agent("create an event tomorrow at 3pm", user="Ruben")` then assert "event" or "created" in resp.lower().

    2. **`test_eval_task_with_deadline_scenario`** ("add task buy milk due friday"):
       - Docstring: "Test add_task with due_at deadline parameter."
       - `if not await llm.is_ready():` — mock LLM returns a tool_call dict for `add_task` with arguments `{"title": "buy milk", "due_at": "<ISO date>"}`. Patch `app.llm.chat` (side_effect: [tool_call, final content]) and `app.tools.call_tool` (return "Added task 'buy milk' with deadline."). Assert mock_call.called and response mentions the task.
       - Real-model path: `resp = await run_agent("add task buy milk due friday", user="Ruben")` then assert "buy milk" in resp.lower().

    3. **`test_eval_priority_task_scenario`** ("add high-priority task review budget"):
       - Docstring: "Test add_task with priority=high parameter."
       - `if not await llm.is_ready():` — mock LLM returns a tool_call dict for `add_task` with arguments `{"title": "review budget", "priority": "high"}`. Patch `app.llm.chat` (side_effect) and `app.tools.call_tool` (return "Added high-priority task 'review budget'."). Assert mock_call.called and response mentions "high" or "priority".
       - Real-model path: `resp = await run_agent("add high-priority task review budget", user="Ruben")` then assert "high" or "priority" in resp.lower().

    4. **`test_eval_weather_refusal_scenario`** ("what's the weather?"):
       - Docstring: "Test graceful refusal for out-of-scope request (weather)."
       - `if not await llm.is_ready():` — mock LLM returns a content-only dict (no tool_calls) with content explaining Nova cannot access weather data. Patch `app.llm.chat` with AsyncMock returning that dict. Assert the response contains "sorry", "can't", or "don't" indicating refusal.
       - Real-model path: `resp = await run_agent("what's the weather?", user="Ruben")` then assert refusal language in resp.lower().

    Insertion order (between existing scenarios and discoverability test): calendar creation first, then task-with-deadline, then priority task, then weather refusal. Use the same `@pytest.mark.asyncio` decorator and `pytest` import already present. Wrap each scenario with `# -- EVAL-01: <scenario> --` comment block separators matching the existing file's style (lines 101-103, 142-144, 191-193).

    Per D-01 (CONTEXT.md): Keep existing 7 scenarios untouched, add exactly these 4. Per D-01: mock pattern uses `app.llm.chat` and `app.tools.call_tool` via `AsyncMock` and `patch` — import already present.
  </action>
  <verify>
    <automated>pytest services/nova-core/tests/test_evals.py -x --tb=short -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    pytest discovers and runs all existing tests + 4 new scenarios; all pass with mocked LLM.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update discoverability test and verify full suite</name>
  <files>services/nova-core/tests/test_evals.py</files>
  <action>
    Update `test_eval_suite_is_discoverable_by_pytest` (around line 216) to reflect the extended suite:

    1. Add assertions for the 4 new function names in the existing set membership check:
       - `assert "test_eval_calendar_creation_scenario" in test_funcs`
       - `assert "test_eval_task_with_deadline_scenario" in test_funcs`
       - `assert "test_eval_priority_task_scenario" in test_funcs`
       - `assert "test_eval_weather_refusal_scenario" in test_funcs`

    2. Bump the minimum count from `>= 7` to `>= 11`.

    3. Run `pytest services/nova-core/tests/test_evals.py -x --tb=short -v` and verify all 11 tests pass.
  </action>
  <verify>
    <automated>pytest services/nova-core/tests/test_evals.py -x --tb=short -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    Discoverability test passes with all 11 scenario names verified; `pytest` collection shows 11 passed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> mocked LLM | No real model invoked in mock path; deterministic, no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | test_evals.py | low | accept | Test file only; no production code modified. Mock patching uses `unittest.mock.patch` context managers — scoped, no side effects leak. |
| T-07-02 | Information Disclosure | test_evals.py | low | accept | Scenarios use generic fixture data (no real user info, no real calendar/email data). Safe for CI logs. |
| T-07-SC | Tampering | pip installs | low | accept | No new packages required; all dependencies (pytest, pytest-asyncio) already present in project. |
</threat_model>

<verification>
1. `pytest services/nova-core/tests/test_evals.py -x --tb=short` passes (all 11 tests)
2. No existing test is modified or deleted — only new functions appended
3. Each new scenario is reachable: `test_eval_suite_is_discoverable_by_pytest` asserts its presence
</verification>

<success_criteria>
- [ ] 4 new eval scenarios added (11 total) in test_evals.py
- [ ] Each scenario has both mock-fallback and real-model paths
- [ ] Discoverability test updated to assert all 11 scenario names
- [ ] `pytest services/nova-core/tests/test_evals.py -x` passes with mocked LLM
- [ ] No implementation files touched
</success_criteria>

<output>
Create `.planning/phases/07-evaluation-suite/07-01-SUMMARY.md` when done
</output>
