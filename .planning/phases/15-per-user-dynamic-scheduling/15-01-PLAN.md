---
phase: 15-per-user-dynamic-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/nova-core/app/scheduler.py
  - services/nova-core/app/channels/dispatcher.py
autonomous: true
requirements:
  - PREF-01
  - PREF-02
  - PREF-03
  - PREF-04
  - PREF-05
  - PREF-06
  - PREF-07
user_setup: []
must_haves:
  truths:
    - Each user's morning briefing toggle and time are read from user_preferences, not hardcoded
    - The weekly briefing fires at the per-user configured day and time
    - Preference changes take effect on the next scheduled send without restart
    - Legacy blanket send_morning_briefing is removed in favor of per-user scheduling
  artifacts:
    - services/nova-core/app/scheduler.py: run_briefing_scheduler reads per-user prefs
    - services/nova-core/app/scheduler.py: legacy send_morning_briefing replaced or deprecated
  key_links:
    - run_briefing_scheduler must not be shadowed by a legacy single-call entry point
---

<objective>
Ensure the briefing scheduler fires per-user configured times and toggles, with no remaining hardcoded-schedule code paths.

Purpose: `run_briefing_scheduler` already reads per-user prefs from `user_preferences`, but the legacy `send_morning_briefing()` (which ignores per-user scheduling) may still be wired. This plan audits the scheduler wiring and verifies end-to-end per-user delivery.

Output:
- Verified that `run_briefing_scheduler` is the only scheduler entry point
- Legacy `send_morning_briefing()` deprecated or replaced with a per-user variant
</objective>

<execution_context>
@/Users/ruben/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/ruben/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@services/nova-core/app/scheduler.py
@services/nova-core/app/main.py
@services/nova-core/app/channels/dispatcher.py
@services/nova-core/static/index.html
@services/nova-core/static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit scheduler wiring and deprecate legacy `send_morning_briefing`</name>
  <files>
    services/nova-core/app/scheduler.py
    services/nova-core/app/main.py
  </files>
  <action>
    The existing `run_briefing_scheduler()` (lines 166-201) already reads per-user `morning_briefing_enabled`, `morning_briefing_time`, `weekly_briefing_enabled`, `weekly_briefing_day`, and `weekly_briefing_time` from `user_preferences`, then calls `send_morning_briefing_for_user(name)` or `send_weekly_briefing_for_user(name)` when the current time matches. This is the correct per-user scheduling path.

    However, the legacy `send_morning_briefing()` (lines 158-163) still exists and iterates all WhatsApp users without checking per-user toggles or times. It is imported in `main.py` line 40 but is NOT registered as an APScheduler job (only `run_briefing_scheduler` is). Still, it remains a risk if imported elsewhere.

    **Changes to `scheduler.py`:**
    1. Replace the body of `send_morning_briefing()` with a call to `run_briefing_scheduler()` so it becomes an alias instead of an independent path:
       ```python
       async def send_morning_briefing():
           """Legacy entry point — delegates to per-user scheduler."""
           await run_briefing_scheduler()
       ```
       This preserves the import in `main.py` (backward compat) while eliminating the stale WhatsApp-only loop.
    2. Add a deprecation comment above the function: `# DEPRECATED: Use run_briefing_scheduler() directly.`
    3. Do NOT remove the import from `main.py` — other dependent code may reference it.

    **Verification steps in task action:**
    - Search for any call to `send_morning_briefing()` outside of scheduler.py itself. Use `rg "send_morning_briefing\(" services/nova-core/ --include '*.py'`. If found only in `main.py` (import) and `scheduler.py`, that's correct.
    - Verify `run_briefing_scheduler` is the only job registered for briefing dispatch in main.py (search for `scheduler.add_job.*briefing` around line 69).
  </action>
  <verify>
    <automated>
      # Verify legacy function no longer has WhatsApp-only loop
      rg -z "get_all_whatsapp_users" services/nova-core/app/scheduler.py && echo "FAIL: legacy WhatsApp loop still present" || echo "OK: legacy loop removed"
      # Verify send_morning_briefing now delegates to run_briefing_scheduler
      rg -c "run_briefing_scheduler" services/nova-core/app/scheduler.py
      # Verify only one scheduler job for briefings
      rg "scheduler.add_job.*briefing" services/nova-core/app/main.py
      # Verify no other file calls send_morning_briefing directly
      rg "send_morning_briefing\(" services/nova-core/ --include '*.py' -l
    </automated>
  </verify>
  <done>
    - `send_morning_briefing()` delegates to `run_briefing_scheduler()` instead of iterating WhatsApp users
    - `run_briefing_scheduler` is the sole scheduled job for user briefings
    - No other code path bypasses per-user preference checks
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify per-user briefing toggle API end-to-end with dashboard</name>
  <files>
    services/nova-core/app/main.py
    services/nova-core/static/app.js
  </files>
  <action>
    The dashboard already renders per-user briefing toggles (morning briefing checkbox+time, weekly briefing checkbox+day+time). The `/api/preferences/briefings` endpoint already saves per-user preferences. This task audits the round-trip for correctness.

    **Audit and verify in `main.py`:**
    1. The `GET /api/preferences` endpoint (around line 654) already returns per-user `morning_enabled`, `morning_time`, `weekly_enabled`, `weekly_day`, `weekly_time`. Verify the response structure:
       - `morning_enabled: bool` defaults to True when NULL
       - `morning_time: str` formatted as `"%H:%M"` 
       - `weekly_enabled: bool` defaults to True when NULL
       - `weekly_day: int` (1=Monday through 7=Sunday)
       - `weekly_time: str` formatted as `"%H:%M"`
       Ensure each user entry includes all these fields. If `channels_enabled` is already returned (from Phase 23), leave it unchanged.

    2. The `POST /api/preferences/briefings` endpoint (line 862) already accepts `BriefingSettingsRequest` with `user`, `morning_enabled`, `morning_time`, `weekly_enabled`, `weekly_day`, `weekly_time` and saves via `UPSERT`. No changes needed unless a field is missing from `BriefingSettingsRequest` model.

    **Audit `app.js`:**
    3. The save-button handler (around line 652) reads all five fields from the DOM and POSTs to `/api/preferences/briefings`. Verify the JS payload matches the expected schema:
       ```javascript
       {
           user: activeSettingsUser,
           morning_enabled: document.getElementById('morning-enabled').checked,
           morning_time: document.getElementById('morning-time').value,
           weekly_enabled: document.getElementById('weekly-enabled').checked,
           weekly_day: parseInt(document.getElementById('weekly-day').value),
           weekly_time: document.getElementById('weekly-time').value
       }
       ```
    4. After save, the handler calls `fetchPreferences()` which updates the UI. Ensure the response from `GET /api/preferences` is parsed and applied to the toggles correctly (around line 215-240 `updateSettingsUI()` area).

    Fix any mismatches found. Do not restyle or refactor existing working code — only correct data-flow issues.
  </action>
  <verify>
    <automated>
      # Check BriefingSettingsRequest model has all fields
      rg "class BriefingSettingsRequest" services/nova-core/app/models.py -A 12
      # Verify the GET /api/preferences returns briefing fields
      rg "morning_briefing_enabled\|morning_briefing_time\|weekly_briefing_enabled\|weekly_briefing_day\|weekly_briefing_time" services/nova-core/app/main.py
    </automated>
  </verify>
  <done>
    - `GET /api/preferences` returns all briefing fields per user (morning_enabled, morning_time, weekly_enabled, weekly_day, weekly_time)
    - `POST /api/preferences/briefings` accepts and persists all five fields
    - Dashboard JS sends correct payload and reflects saved values after `fetchPreferences()`
    - Toggle changes take effect on the next 60-second scheduler tick (no restart needed)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard browser → Nova Core API | LAN-only, accepts scheduling preferences |
| Nova Core → Telegram/WhatsApp | Outbound proactive pushes to user devices |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | POST /api/preferences/briefings | low | accept | LAN-only; rogue LAN actor could reschedule briefings but not access sensitive data |
| T-15-02 | Denial of Service | Scheduler flooding | low | accept | Briefing jobs are user-gated; max 2 pushes per user per minute (morning + weekly) |
| T-15-SC | Tampering | pip installs | medium | mitigate | No new packages — apscheduler, asyncpg, httpx already in project |
</threat_model>

<verification>
1. `rg "scheduler.add_job" services/nova-core/app/main.py` — shows only one briefing job (`run_briefing_scheduler`)
2. `rg "get_all_whatsapp_users" services/nova-core/app/scheduler.py` — zero matches (legacy loop removed)
3. `pytest services/nova-core/tests/ -x -q` — all existing tests pass (no regression)
</verification>

<success_criteria>
1. `run_briefing_scheduler()` is the sole entry point for scheduling briefings
2. Legacy `send_morning_briefing()` delegates to `run_briefing_scheduler()`
3. Dashboard toggles and time pickers persist per user via `/api/preferences/briefings`
4. Changed preferences take effect on the next 60-second scheduler tick
5. All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/15-per-user-dynamic-scheduling/15-01-SUMMARY.md` when all tasks complete
</output>
